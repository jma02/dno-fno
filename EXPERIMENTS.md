
### Experiments

Session rule: append every meaningful experiment here (or an appropriate file) with correctness, timing, and merge decision.

**Note on backfilled rows:** rows whose `Decision` column begins with `BACKFILL from notes/<file> —` were reconstructed post-hoc by scanning `notes/` (design docs, session logs, LaTeX reports) after this table was in place. Numbers and dates in those rows are as reported in the source note; `Time` is `--:--` because exact clock time was not recorded. Backfilled rows live in a dedicated **Backfilled from notes/** section at the bottom of this file to keep the main table's provenance clean.

Logging format:
- Use local `HH:MM` time from `date` when adding new rows.
- Use `--:--` only for older rows where exact time was not recorded; do not invent precision.
- Fill both `Motivation` and `What Tried / Evidence`.
- `Motivation` should say why this was plausible before running it.
- `What Tried / Evidence` should say what changed and what the result taught us.
- Keep timings in `HH:MM` format.

| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-01 | 21:13 | v8 data-coverage audit for mid-k Tanaka failures | Determine whether the learned `k=64:128` instability reflects missing physical/off-manifold training states and whether a data intervention should precede the proposed closed-loop tangent penalty. | Read v8 composition and randomly sampled 20,000 physical Tanaka rows with `h>=0.23`. Tanaka joint coverage at proxy `h>=0.23, max|eta|/h>=0.27` is approximately 1.20% of Tanaka rows (~24k), only 0.32% of all 7.43M v8 rows. In that subset, eta `k=64:128` RMS has median/p99/max `3.56e-8/2.31e-7/3.87e-6`; v8 g0 cid5 prediction reaches `3.93e-7` at t=128, `2.58e-6` at 132.8, and `8.10e-5` at 136, then `max|eta_x|=0.576` versus sampled physical max `0.141`. Source-13 shallow-steep-wide supplies large-slope controls but negligible mid-k content. Existing `steep_tanaka_v2` shards target the exact physical corner but contain zero samples. Historical targeted `tanaka_perturbed_v2` fine-tuning eliminated g0 NaN/divergence (0/16) but worsened median final eta error `0.0420 -> 0.0761`; its broad modes 1–128, relative amplitude `1e-3:3e-2` perturbations were much stronger than ignition. Generic model-drift fine-tuning retained 2/16 g0 NaNs and worsened median error. | CPU-only raw-row sampling from STORED v8 arrays; fixed seed. Joint count and spectral extrema are estimates from 20k of 162,600 deep Tanaka rows. Historical comparisons use saved matched f64-harness summaries, though perturbed v2 was fine-tuned from v3 rather than current v8.5b and only g0 was evaluated. | 00:08 CPU | DATA GAP PLAUSIBLE and already partially validated. Before implementing expensive tangent training, fine-tune v8.5b with a small pack concentrated at `h=0.23:0.30, a/h=0.27:0.35` plus `k=64:128` perturbations at relative RMS `1e-6:1e-3`, mixed at 5–10%. Clean steep-Tanaka data alone may reduce the seed; targeted off-manifold pairs are needed for recovery. Reject broad perturbations/model-drift replay as currently configured. |
| 2026-07-01 | 20:57 | v8 g0 cid=5 GL2 fixed-point residual trace on CPU | The NPZ audit implicated an unchecked four-iteration GL2 stage solve in the terminal jump. Directly measure each stage-update residual to determine whether four iterations are merely inaccurate throughout or whether the fixed-point map specifically loses contractivity at the cascade endpoint. | Evaluated relative two-stage update residuals at saved states, then replayed the production four-iteration step from the last finite saved state at t=138.4 with `dt=0.01`. Four iterations are adequate during healthy rollout: fourth residual is `2.35e-9` at t=0 and `4.74e-8` at t=138.4, inside JCP09's reported `1e-6`–`1e-8` range. It then rises monotonically: `1.31e-7` at 138.80, `3.93e-6` at 138.90, `4.90e-4` at 138.97, and `5.85e-3` at 138.98. At 138.99 the successive residuals increase `0.190 -> 0.282 -> 0.469 -> 0.768`, directly demonstrating a non-contractive fixed-point map; that step sends `max|eta| 0.110 -> 6.01` and `max|xi| 0.0627 -> 67.9`. The next step is NaN. | Replayed from the production f64-harness NPZ state using the production `_gl2_if_step_surrogate` on JAX CPU. Starting from float32-saved t=138.4 shifts failure from the production saved frame at 139.2 to 139.0, but reproduces the same terminal cascade and gives an unambiguous within-step residual sequence. | 00:38 wall, CPU only | CONFIRMED terminal mechanism. A fixed count of four is fine while the map is contractive; adding iterations after residuals start increasing would make this failure worse. Add a residual/contraction check and reject/retry the substep at smaller `dt`; treat that as containment, while training must still remove the earlier learned k=64:128 feedback that drives the state into this regime. |
| 2026-07-01 | 20:19 | CPU NPZ NaN-cascade audit: v8, v8.5b tied, and v8 FFT-fp64 Tanaka g0/g1 | Saved trajectories should distinguish a gradual learned-operator instability from an arithmetic singularity or a one-step integrator failure, while matched variants test whether tying or FFT precision changes the mechanism. | Read six `*_trajs.npz` files (96 trajectories, 8 NaNs) plus v8 g0 cid=5 `trace_cid5.npz`. Every failure makes all 1024 values of eta, xi, and gxi NaN in the same saved frame. Before failure, eta and gxi energy grows persistently in k=64:128; e.g. v8 cid=5 eta-band RMS grows `2.08e-5 -> 1.55e-3` over its last 5.6 s and max `|eta_x|` grows `0.139 -> 1.32`. The fine trace is finite at t=137.08 (`max|eta|=0.172`, `max|gxi|=14.8`) but one dt=0.01 GL2 step later eta/xi jump to `3.6e18/7.3e18` and gxi becomes NaN. This is incompatible with the preceding `max|eta_t|=16.5` under a converged step. The code always performs four fixed-point updates with no residual test; JCP09 instead reports 2–4 iterations **within relative error 1e-6–1e-8**. A truth-free diagnostic using eta k=64:128 RMS `A`, initial six-frame median `A0`, and six-frame log-growth slope caught 8/8 NaNs with 0/88 false alarms when `A>1e-4`, `A/A0>100`, and slope `>0.18 s^-1`; lead time was 1.6–7.2 s. | Read-only NumPy analysis on CPU. Same signature occurs in baseline, self-adjoint-tied, and FFT-fp64 rollouts, so it is not specific to one checkpoint. Detector thresholds are diagnostic only and were evaluated on this 96-trajectory sample, not held-out data. | 00:14 CPU | CONFIRMED two-stage mechanism: learned mid/high-k feedback drives the state out of distribution, then the unchecked GL2 fixed-point solve loses convergence and creates the terminal one-step blow-up. Next isolate causality by logging per-iteration stage residuals and trying tolerance-controlled iteration/substep rejection; do not treat filtering or precision alone as the cure. |
| 2026-07-01 | 19:58 | v8.5b self-adjoint multiplier tying result audit | JCP09 relies on self-adjointness of the DNO, and `--cs_tie_xi_out_mult` enforces the corresponding `M* Phi M` structure while removing 24% of CS-DNO parameters. Determine whether the completed but unlogged v8.5b run improved long rollouts despite its slightly worse one-step validation loss. | Compared matched f64-harness Tanaka summaries for v8 and `cs_dno_w512b8_l256_v8p5b_tiexo_20260622_234309`. g0: median final eta error `0.01632 -> 0.01305` (-20%), p95 `0.3117 -> 0.2138` (-31%), mean gxi error `0.1566 -> 0.1094` (-30%), median energy drift `0.001565 -> 0.000829` (-47%); NaN rate stayed 2/16. g1: median eta error `0.09484 -> 0.04693` (-51%) and NaN/divergence `1/16 -> 0/16`, though median energy drift worsened `0.00128 -> 0.00367`. One-step best val was 8% worse (`0.001396` vs `0.001288`), showing rollout structure is not selected by global val loss alone. | Same v8 dataset, 40 epochs, architecture/training settings, and f64 Tanaka harness; only multiplier tying differs. Existing g1 summary is valid though the aggregate `all_summaries.json` stopped after g0. | Existing completed runs; analysis <1 min | ACCEPT self-adjoint tying as the new model baseline. Before shipping globally, run the missing full 10-regime suite to check BF/random-sea regressions. |
| 2026-07-01 | 19:58 | Zero-mean / mass-invariant audit on saved Tanaka g0 rollouts | The periodic DNO satisfies `mean(G(eta)xi)=0`; if the learned residual leaked a DC component, accumulated volume drift could explain coherent rollout error and suggest an exact architectural projection. | Measured spatial means in saved v8, v8.5b tied, and v9-jacreg Tanaka-g0 trajectories. Inference already returns `gxi - mean(gxi)`. Finite predicted `|mean(gxi)|` p95 was `1.4e-10`–`2.3e-10`; final `|mean(eta)-mean(eta_0)|` p95 was `0.9e-10`–`4.0e-10`, versus median final amplitudes around `1.1e-2`. | Read-only diagnostic on production trajectory files; result agrees with `build_predict_gxi*` mean projection in `model_rollout.py`. | <1 min CPU | REJECTED as a model-improvement axis: DC leakage is already projected out and is 8+ orders below the state scale. |
| 2026-06-29 | 01:11 | Cascade gate (C) impl + wiring (eval-time runtime intervention) | v8 cs_fft_fp64 retrain landed (val 0.00128 ep40) but FAILED on rollout: NaN g0/g1 = 0.125/0.0625 identical to fp32 baseline, tanaka η_med_tf and p95 WORSE. f32 chained-FFT floor hypothesis falsified at 100% data. Pivot from preventing-cascade-via-training to detecting-and-suppressing-at-rollout. Conditional Hou-Li gate inside surrogate substep: when η high-k energy ratio r = E_hi/E_lo crosses a threshold, sigmoid-blend a Hou-Li smoothing onto the state. JIT-safe (sigmoid in log-r); no-op on healthy state. | Added `_apply_cascade_gate` helper in `solver/evals/model_rollout.py` + 8 cascade_* kwargs through `_gl2_if_step_surrogate` and `rollout_surrogate`. Wired same kwargs through `surrogate_rollout_per_ic` and `run_regime` in `solver/evals/eval_suite.py`; CLI flags `--cascade_gate --cascade_k_cut --cascade_r_threshold --cascade_sharpness --cascade_houli_a --cascade_houli_m --cascade_k_eff --cascade_no_filter_xi`; output-dir tag `_cg_kc{kc}_rt{rt}` and settings persisted in `*_summary.json`. CPU smoke: gate ON vs OFF on clean random state differ by 8.7e-19. Calibrated from v8 fp64 tanaka_g0 trajs (NB=16): truth median r at k_cut=32 is 6.4e-9, pred median 1.5e-6, cascading cases hit 0.22. Default `r_threshold=1e-3 sharpness=10 k_cut=32 houli_a=0.69 houli_m=4 k_eff=128` sits 7 orders above clean and 2 below cascade. | n/a (eval-time) | IMPLEMENTED; test queued (task #101) for after PIDs 2344236/2344237 finish (~1-2h). Also queued architectural retrain w/ smooth output cutoff (task #99, A). |
| 2026-06-25 | 01:26 | Full v8 cs_fft_fp64 retrain (LAUNCHED — locked local 2-GPU) | A/B on 10% data slice (rows 18:21 + 19:33 below) settled it: cs_fft_fp64 hit val 0.003504 at ep20 vs fp32 baseline 0.006746 at ep20 (−48%), both still descending at ep20. The fp32 "floor" of v8 was real — the chained-FFT rounding noise in the spectral path was capping how cleanly the model could fit. With the spectral path widened to fp64/complex128, the optimizer has access to lower-loss regions that f32 couldn't represent. | `JAX_PLATFORMS=cuda nohup uv run python train-jax-10m/1d_dno_fno_jax.py --model cs_dno --norm scale --dataset combined_dataset_v8.npz --modes 64 --width 512 --n_blocks 8 --latent 256 --sobolev_k 1 --batch_size 256 --lr 0.0002 --weight_decay 0.0001 --epochs 40 --cs_n_polys 3 --cs_mult_hidden 128 --cs_fft_fp64 --run_name cs_dno_w512b8_l256_v8_fftfp64_20260625_012616`. PID 1060059 / child 1060062. Full 100% v8 data, 40 epochs, batch 256, 2 RTX 6000 Ada. Success criteria (now sharper post-A/B): (1) val_loss meaningfully below the v8 fp32 baseline ~7e-4; (2) tanaka_g0/g1 f64h NaN tail drops from 2/16+1/16; (3) high-k gξ noise at clean IC drops materially. **COMPLETED 2026-06-26 23:36** (46h10m wall). Final val ep40 = **0.00128**, train = 0.0013. Trajectory ep30→ep40: 0.0015, 0.00151, 0.00140, 0.00140, 0.00138, 0.00133, 0.00133, 0.00132, 0.00132, 0.0013, 0.00128 — monotone descending envelope, still descending at final ep. vs A/B arm A (fp32, 10% slice, ep20 final 0.00675): **−81%**. vs A/B arm B (fp64, 10% slice, ep20 final 0.00350): **−63%**. vs v8 fp32 full baseline ~7e-4: still ~1.8× above (so even more headroom if extended). Post-train auto-eval hit the known #21 ScopeParamNotFoundError on input_proj — non-fatal; manual eval queued as task #98. | ~41h expected | RUNNING |
| 2026-06-24 | 19:33 | A/B arm B: cs_fft_fp64 on 10% v8 slice, 20ep | Decide whether the prior full cs_fft_fp64 launch (row 17:40, killed step 1700) was worth its 41h wall cost before re-committing. Cheap A/B on 10% data, 20 epochs vs identical fp32 baseline. | Sequential pair: arm A (fp32) first, arm B (`--cs_fft_fp64`) second, both with `--data_fraction 0.1` and otherwise identical hparams. **Arm B best val 0.003504 at ep20/20 (final)**. Trajectory ep1→ep20: 0.0117 → 0.00858 → 0.00785 → 0.00857 → 0.00684 → 0.00765 → 0.00653 → 0.00531 → 0.00530 → 0.00504 → 0.00521 → 0.00461 → 0.00467 → 0.00399 → 0.00409 → 0.00381 → 0.00365 → 0.00355 → 0.00353 → 0.00350. Still descending at ep20 (no plateau). | 132 min wall (6.6 min/epoch steady-state, 3.4× fp32 cost) on local 2-GPU | **DECISIVE WIN vs arm A**. Best val 48% below fp32 arm; arm B already crossed arm A's BEST val (0.00675) at ep7. Triggers full-data retrain (row 01:26 above). |
| 2026-06-24 | 18:21 | A/B arm A: fp32 baseline on 10% v8 slice, 20ep | Control arm for the cs_fft_fp64 A/B — fixes hyperparameters and data slice so the only difference is the FFT precision flag. Added `--data_fraction` flag to trainer (single-line index slice after `build_split_indices`). | Same launch as arm B but without `--cs_fft_fp64`. PID 895393. Trajectory ep1→ep20: 0.0128 → 0.0119 → 0.0118 → 0.0105 → 0.0105 → 0.00944 → 0.00889 → 0.00881 → 0.00816 → 0.00829 → 0.00778 → 0.00805 → 0.00732 → 0.00725 → 0.00728 → 0.00692 → 0.00688 → 0.00680 → 0.00676 → 0.00675. **Best val 0.006746 at ep20/20 (final)**. Post-train dno_dataset eval failed with the known #21 ScopeParamNotFoundError on input_proj — non-fatal. | 68 min wall (3.4 min/epoch steady-state) on local 2-GPU | Control set. |
| 2026-06-22 | 11:53 | state-Hou-Li tanaka eval (a=m=36, ff=0.25) on v8 + v8.5a | JCP09 Tier 1B "Apply to state": hypothesis = baseline-cliff at k=128 spills aliased high-k into state every step; smoothing the state filter should break the cascade. | Threaded filter_shape/houli_a/houli_m through model_rollout `_rhs_nonlinear_surrogate`+`_gl2_if_step_surrogate`; ran both v8 and v8.5a with state-Hou-Li (gxi-Hou-Li on v8.5a too). | g0 v8 baseline 2 NaN / 5 FAIL → v8 houli 1 NaN / 6 FAIL (cid=11 came out of NaN as ratio 1.03, still FAIL). g0 v8.5a base 3 NaN / 5 FAIL → v8.5a houli 1 NaN / 7 FAIL. g1 identical NaN+FAIL pre/post on both models. Ratios on already-failing deep cases (cid=0,2,7,12-14) unchanged to 2 sig figs. | ~26 min surrogate / 21 min truth per regime per GPU | REJECTED for cure — Hou-Li at a=m=36 ff=0.25 converts NaN→bounded-garbage but does not stop the cascade. CAVEAT: transition <1 mode wide ≡ hard cutoff at these params; true-smooth m=4 shape *not* refuted by this test. |
| 2026-06-22 | 16:40 | state-Hou-Li tanaka eval (a=0.69, m=4, ff=0.25) on v8 + v8.5a | True-smooth Hou-Li: c=0.5 at k=128, transition over k~96–160. Test whether the SHAPE of the cutoff (smooth taper vs cliff) breaks the cascade — caveat from m=36 run that needed addressing. | Same args wiring as the m=36 run, just `--houli_a 0.69 --houli_m 4`. Two parallel evals (v8 GPU0, v8.5a w/ filter_gxi+gxi-Hou-Li GPU1). Half the eval ran with samzheng's fm_unet sharing GPUs (2x slowdown on truth/surrogate phases). | g0 v8 base 2 NaN / 5 FAIL → v8 m4 **0 NaN / 9 FAIL**. g0 v8.5a base 3 NaN / 5 FAIL → v8.5a m4 0 NaN / 9 FAIL. g1 v8 1 NaN / 4 FAIL → v8 m4 0 NaN / 8 FAIL. g1 v8.5a 2 NaN / 3 FAIL → v8.5a m4 1 NaN / 7 FAIL. Pattern: m=4 cures NaN but pushes previously near-zero ICs (cid=4,8,10,14,15 on g0; cid=1000002,3,11 on g1) into FAIL zone (×10–×100 ratio jump). η_p95_tf rose from ~0.3 to ~1.2 across the board. filter_gxi+gxi-Hou-Li adds nothing on top of state-Hou-Li (v85a_m4 ≈ v8_m4 to 1%). | ~50 min surrogate / 45 min truth per regime per GPU on shared box | REJECTED for cure — boundary-cliff hypothesis falsified at both ends (hard ≡ m=36 AND m=4). Smoothing the state filter trades catastrophic NaN for "wrong-but-bounded across the board" because model relies on mid-k content (k~64–128) that m=4 attenuates. Cascade is in the learned mid-k representation, not in filter shape. Pivot: v8.5b retrain with `--tie_xi_out_mult` only (other single-knob axis from doc Tier 1C). |
| 2026-06-24 | 17:15 | v8 G1+kcut=128 fp32 retrain (BELAYED 10 min in) | Same motivation: per-band fit_error_growth_cid5 power-law signature in the carrier band motivated G_1 baseline as a coherent-drift fix. The kcut mitigation was queued to test. | Launched 17:15 with `--cs_use_g1_baseline --cs_g1_k_cut 128`, PID 856375. Killed at 17:25 (~10 min, still in dataset-load phase, no training stdout yet) on user direction to belay G_1 entirely and instead retrain v8 architecture (no G_1) with a mixed-precision FFT casting system that attacks the f32 noise floor at the source. See follow-up row 17:40. | --:-- | KILLED — superseded by mixed-precision retrain (no G_1) |
| 2026-06-24 | 17:40 | v8 cs_fft_fp64 mixed-precision retrain on local 2-GPU (LAUNCHED) | The spectral-error abs panel (`spectral_error_cid5_abs.png`) showed model gξ has real generative noise in k[64-128) and k[128-256) at t=0 from a clean IC — flat-spectrum, not a relative-metric artifact (truth ~5.8e-4 at k[64-128) for η, model err 1.59 at the last finite frame; truth ~4e-7 at k[256-513) for gξ, model err 0.87 absolute). The prior G_1-program memory diagnosed this kind of pattern as f32 chained-FFT rounding noise amplified ~k². Two attacks on it: full fp64 (existing `--precision fp64`, 2x wall + 2x memory) or mixed-precision casting (params/Dense/optimizer stay fp32; only the spectral compute path is widened to complex128). User chose mixed-precision. Implementation: new field `fft_fp64` on CraigSulemDNO + CraigSulemBlock, casts at entry/exit of every spectral block (CraigSulemBlock's rfft→mult→irfft→...→irfft, `_linear_baseline`, `_eta_spatial_features`, and `_g1_baseline` if on). Also added `--cs_fft_fp64` CLI flag (enables jax_enable_x64 too), config persistence, and rollout-loader plumbing in `solver/evals/model_rollout.py` so checkpoints auto-enable x64 at inference. Smoke test on CPU confirmed: with fft_fp64=True, output stays fp32 (cast back at boundaries), params stay fp32; required one fix to `residual = jnp.zeros((B, N))` which silently defaulted to fp64 under x64=True. | Launched: `JAX_PLATFORMS=cuda nohup uv run python train-jax-10m/1d_dno_fno_jax.py --model cs_dno --norm scale --dataset combined_dataset_v8.npz --modes 64 --width 512 --n_blocks 8 --latent 256 --sobolev_k 1 --batch_size 256 --lr 0.0002 --weight_decay 0.0001 --epochs 40 --cs_n_polys 3 --cs_mult_hidden 128 --cs_fft_fp64 --run_name cs_dno_w512b8_l256_v8_fftfp64_20260624_174059`. PID 874013 / child 874017. Both GPUs idle pre-launch. Expected ~15-18h wall (fp64 FFTs ~2× slower but no param widening). **REVISED at 17:48 from live measurement**: 6.25 it/s steady-state at step 1634/23231 → 62 min/epoch → **~41h for 40 epochs (~3.4× v8 baseline, not 2×)**. Modal H100:2 fp64 baseline at 12.2 min/epoch ($73/40ep, per [[project-fp64-modal-timing]]) is the obvious mid-run pivot if local wall becomes binding. Success criteria: (1) tanaka_g0/g1 f64h rollout NaN tail drops from 2/16+1/16 (cascade fuel attacked at source); (2) high-k gξ absolute magnitude at clean IC drops materially vs v8 baseline (the spectrum-noise-floor goes away). Median-η improvement is a secondary expectation — coherent drift wasn't attacked here, only the noise floor. | TBD ~15-18h | PENDING |
| 2026-06-24 | 16:49 | Eval-time Hou-Li smooth filter at k_eff=64 on v8 tanaka (state + gxi paths) | Per-substep trace + spectral-error heatmap pinpointed the cascade fuel as the model's flat-spectrum gξ output in k[64–128] from t=0 on a clean IC. Prior Hou-Li tests cut at k_eff=128 (didn't touch the noisy band). Test: push the smooth m=4 taper down so half-power sits at k=64 and the cascade band is attenuated 50–100%. Cheap eval-time workaround before any retrain. | Added `--filter_fraction` CLI override to eval_suite (overrides the regime's default 0.25). Ran `--filter_gxi --filter_shape houli --houli_a 0.69 --houli_m 4 --filter_fraction 0.125 --f64_harness` on v8 baseline, tanaka_g0 on GPU 0 + tanaka_g1 on GPU 1 in parallel. **g0**: NaN 0.125 → **0.00**, η_med_tf 0.0107 → 0.0258 (×2.4), η_p95_tf 0.21 → 1.18 (×5.6), div_rate 0.125 → 0.21. **g1**: NaN 0.0625 → **0.00**, η_med_tf ~0.047 → **1.046 (×22)**, η_p95_tf ~0.36 → 1.30, div_rate ~0.06 → 0.54 (8.6/16 cases). | ~21 min truth / ~27–29 min surrogate per regime per GPU | REJECTED. NaN cured on both regimes, but median accuracy craters — especially on g1 (shallow water) where the carrier's harmonics are pushed up in k by the depth scaling, so cutting at k=64 destroys real signal. Closes the eval-time-filter-cutoff axis: no cutoff cleanly separates cascade fuel (model's flat-spectrum gξ noise) from Tanaka harmonics — they overlap in k[32–128]. Cure must come from the model output distribution at training time (G_1 baseline retrain, fp64 retrain, or spectrum reg). |
| 2026-06-24 | 15:14 | NaN-cascade per-substep trace on tanaka_g0 cid=5 (v8 baseline, f64h) | Close the mechanism: produce a substep-by-substep narrative of how the model goes from a clean IC to NaN at the production rollout's t=139.2. Prior partial trace was off by 4× in NaN time because the local script had drifted from `rollout_surrogate`. | New trace at `solver/evals/trace_nan_cascade_fast.py`. Two bugs reproduced+fixed: (a) used `meta["dt"]=0.08` (npz save dt) instead of eval's `outer_dt=0.8` — inner_dt was wrong by 10×; (b) missing `ti.project_zero_mean_xi` between substeps, which production calls after every substep (model_rollout.py:331-333). Initially built as a re-implemented chunked scan (NaN at substep 14280, t=142.8) — then **refactored**: `rollout_surrogate` now emits `"gxi"` alongside `"eta"`/`"xi"` (non-breaking dict add at model_rollout.py:299-341), and the trace is a thin shell calling `rollout_surrogate(substeps=1)` over a fine outer-dt grid + one jit'd derivative-math diagnostics pass — no duplicated integrator, no model re-inference, no chunking. After refactor NaN lands at substep 13709 (t=137.09); ~570-substep shift from prior chunked run is XLA fusion-order sensitivity at a near-singular trajectory (the gxi emission perturbs op ordering by ε; cascade is exponentially sensitive). Both bracket production's t=139.2 within 2s (~1.5%). **Cascade profile (cid=5)**: t=0–96 noisy attractor: gxi k[64-128] holds at ~3.5e-3, eta k[64-128] ~3e-4, max\|gxi\|~6.6e-2. t=96–124 slow ramp: mid-k gxi 3.5e-3→1.1e-2 (×3 over 28s), eta mid-k 3e-4→1.4e-3. t=124–137 superlinear: doubles per ~4s window. Last 8 substeps before NaN: max\|gxi\| 0.40 → 0.51 → 0.68 → 1.00 → 1.65 → 3.45 → 14.8 → NaN. Terminal blow-up is in model gxi (out-of-distribution η), NOT in `(1+ηx²)` — denom grows (regularizes) but `num=gxi+ηx·ξx` outpaces it. Mid-k dominates throughout the cascade; at the last finite frame gxi k[64-128]=952 vs k[0-32]=345. Trajectory data saved to `outputs/cs_dno_w512b8_l256_v8_2gpu_20260619_024621/trace_cid5.npz`. | ~3 min wall on GPU 1 under shared load (refactored version) | Cascade mechanism CONFIRMED: slow mid-k feedback (gxi→eta high-k→ηx²→model OOD→larger mid-k gxi) → singularity once \|eta\| exceeds training distribution. Both retrain motivations (fp64 + gxi-filter) target this loop. Note: eval_suite.py:367 still does its own `jax.vmap(predict)(out["eta"], out["xi"])` to recover gxi — now redundant since `rollout_surrogate` emits it directly; can be deleted in a follow-up. |
| 2026-06-24 | 01:09 | NaN-cascade per-substep trace on tanaka_g0 cid=5 (DEFERRED) | Visualize where the f64-harness NaN ignites in the GL2-IF + cs_dno op chain. Prior fp32 CPU trace (s=0–190) already showed model gxi has spurious k[64–128] energy ~1.29e-3 at t=0 vs eta high-k 4.20e-4, growing ×5 over 180 substeps with xi_t × 16 and (1+ηx²) × 56 in the same band — cascade clearly in progress but NaN substep not yet reached. Want f64 harness to match production rollout exactly. | New script at `solver/evals/trace_nan_cascade.py` walks each inner substep, prints band-resolved spectra (5 k-bands) of η, ξ, gξ, η_t, ξ_t, ηx·ξx, gξ+ηx·ξx, 1+ηx². Tried CPU+f64 → LLVM section-mmap denied. Root cause: `vm.overcommit_memory=0`, CommitLimit=268 GB, Committed_AS=216 GB; v8.5b training alone holds ~100 GB committed; XLA's ContiguousSectionMemoryManager can't get a fresh PROT_RWX arena. Tried GPU-1 f64 → 24-min compile under v8.5b + samzheng GPU contention with no Python output yet. | --:-- | DEFERRED until v8.5b training + eval are done. Re-run then on a quiet GPU; if commit-pressure still hot, `sudo sysctl vm.overcommit_memory=1` first. Mechanism hypothesis already captured by fp32 trace; f64 run is for substep-of-NaN confirmation and op-by-op narrative. |

---

## Backfilled from notes/

The rows below were reconstructed post-hoc by mining `notes/` (session logs, LaTeX reports, ablation studies, dataset audit JSON). Every `Decision` cell starts with `BACKFILL from notes/<file> —` so provenance is explicit. `Time` is `--:--` throughout because clock time was not recorded in the source notes. Numbers, dates, and val_loss values are as reported in the source note — no re-verification.

| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-03-31 | --:-- | Explicit IF-RK4 on fast soliton family `f*` | Explicit integrating-factor RK4 was the default for all soliton families; test whether it holds up on the coarse-time-grid `f*` cases (`dt=1.0` vs `s*`'s `dt=0.2` and `h005005`'s `dt=0.08`). | Ran explicit IF-RK4 on `f*` cases (`coll.anim_f04005` horizon) with aggressive substepping. Blew up. Implicit midpoint in IF variables was worse. Gauss-Legendre 2-stage 4th-order implicit RK better but still unstable alone. Adding a spectral cutoff stabilized the fast cases. Matches JCP09 (Xu & Guyenne) which uses implicit symplectic 4th-order RK + de-aliasing, not plain explicit RK4. | Import + `python -m compileall solver` clean; GPU rollout on `coll.anim_f04005` confirmed instability of explicit path. | --:-- | BACKFILL from notes/solver_session_2026-03-31.md — REJECTED explicit IF-RK4 for `f*` family; ACCEPTED `gl2_if` + spectral filter as fast-case default. |
| 2026-03-31 | --:-- | Fast-case recipe: `gl2_if` + substeps=8 + filter_fraction=2/3 + DNO order 6 | Combine implicit GL2 RK + spectral filter into a single production recipe for the fast soliton family through its full production horizon. | Rolled out `coll.anim_f04005` end-to-end with `method=gl2_if`, `internal_substeps=8`, `filter_fraction=2/3`, `dno_order=6`. Stable through the full horizon; long-time accuracy still rough (open issue). | Full 30-file soliton batch reran successfully under these defaults. | 2210.87 s (~36.8 min) for all 30 soliton files on GPU 1 | BACKFILL from notes/solver_session_2026-03-31.md — ACCEPTED as `fast_method`/`fast_substeps` defaults in `solver/evals/render_all_soliton_rollouts.py`. |
| 2026-03-31 | --:-- | Xi gauge / spatial-mean drift diagnostic on mild soliton rollouts | Rollouts showed raw `xi` "looking poor" despite otherwise-clean dynamics; test whether the badness is a zero-mode gauge issue rather than a real dynamical error. | On mild soliton rollouts, raw `xi` looked poor while `xi - mean(xi)`, `xi_x`, and `G(η)ξ` all looked clean. Interpretation: additive spatial-mean drift in `ξ` (a gauge mode); physically observable quantities are insensitive to the DC component. Prefigured the DC projection now baked into `build_predict_gxi*`. | Qualitative visual comparison on mild-soliton GIF batch. | --:-- | BACKFILL from notes/solver_session_2026-03-31.md — ACCEPTED as rec: project out zero Fourier mode of `ξ` during rollout/eval (later baked in). |
| 2026-03-31 | --:-- | Full 30-file soliton GIF batch rerun under new defaults | Regenerate all soliton rollout GIFs under new `rk4_if` default for `h*`/`s*` and `gl2_if` fast-path for `f*`, to verify the reorganized `solver/` package still produces correct outputs end-to-end. | Reran all 30 files with `dno_order=6`, `gl2_if` for `f*`, `rk4_if` for `h*`/`s*`, `fast_substeps=8`, `filter_fraction=2/3`. Batch completed successfully; outputs in `soliton_rollouts/`. Dataset: 30 waveform files, `Nx=1024`, `L=164`, 1001 saved time slices per file. | Import smoke + full batch completion under project venv. | 2210.87 s wall on GPU 1 | BACKFILL from notes/solver_session_2026-03-31.md — ACCEPTED; outputs adopted as reference set. |
| 2026-05-02 | --:-- | v2 squiggle root-cause: symmetric-center soliton control | Rule out DNO-solver instability at `h_ref ≳ 0.2` on `L=2π` as the cause of the high-k squiggles in `depth_max=0.30` smoke output. | Single-soliton rollouts at `h_ref ∈ {0.05,0.10,…,0.30}`, placed **symmetrically at `x=L/2`**, `α=a/h=0.20`, `tmax=20`. Band `k∈[80,128)` energies `t=0→t=20`: `h=0.05`: `1.4e-10→1.0e-10`; `h=0.10`: `7.6e-13→3.0e-13`; `h=0.15`: `1.4e-10→7.5e-11`; `h=0.20`: `9.7e-9→4.4e-9`; `h=0.25`: `1.1e-7→6.3e-8`; `h=0.30`: `4.9e-7→2.3e-7`. All slightly decaying. | Symmetric-center solitons stable to h=0.30; broken c5 case had `h=0.276` at `center=4.944`, not `L/2`. | short sweep | BACKFILL from notes/v2_squiggle_investigation.tex — REJECTED solver-instability hypothesis; failure is IC-side (tail truncation at periodic boundary). |
| 2026-05-02 | --:-- | v2 IC-builder fix: 3-copy Poisson vs zero-extension | The truncated `jnp.interp(left=0,right=0)` in the IC builder dropped soliton tails at the periodic boundary → step discontinuity with `1/k` Fourier tail × `k⁶` DNO amplification = the squiggle. Test whether `depth_max=0.08` + 3-copy Poisson wrap kills the noise. | Rebuilt c5 IC (`h=0.276`, `center=4.944`) under OLD vs NEW: OLD had step `|η(0)−η(2π⁻)|=4.7e-3` = 11% of peak, `1/k²` spectrum, `|η̂|²≈10⁻³` at `k=100`. NEW: exponential decay, `|η̂|²<10⁻⁹` by `k=50`. Band energy at t=0 in `k∈[80,128)`: OLD `6.3e-3`, NEW `2.5e-7` (ratio `2.5×10⁴`). Full-range 16-case verify at `dt=0.01, tmax=200`: max band growth `1.03×` vs `3×` before. Sign flips: max 16, p99 8, p95 6, median 4 (broken d03 had hundreds at `t≈10`). | OLD c5 t=0 band matches broken dataset's `6.4e-3` → closes chain from IC bug to rollout symptom; new build matches truth energy to `1e-15` relative. | ~3h 32min on single H100 for 16-case verify | BACKFILL from notes/v2_squiggle_investigation.tex — ACCEPTED `depth_max=0.08` + 3-copy Poisson wrap as v2 IC builder fix. |
| 2026-05-02 | --:-- | v2 stress-grid: single-soliton `(h, α)` failure boundary | Map where the fixed solver breaks so production cap sits with margin. Sweep `(h_ref, α=a/h)` beyond production cap; `tmax=30`, single soliton, random off-center. | 7×4 grid. Sign-flip metric bounded 2–4 (misses failure). `k[80,128)` growth `t=30/t=0`: safe at (`h≤0.25, α≤0.40`) with ratios 0.4–1.2; corner (`h=0.30, α=0.40`) jumps to `2.74×`; (`h=0.40, α=0.40`) to `19.25×`. Follow-up `tmax=50` at borderline corners: `h=0.30/α=0.40` → `26.83×`; `h=0.40/α=0.40` → `1186×` (would NaN at `tmax=200`). Multi-crest sum-budget at `h∈{0.05…0.20}, α_sum≤0.40` all in 0.5–1.1×. | Production (`depth_max=0.08, steepness_max=0.35, 1–3 crests`) sits ~3× inside strictest tested boundary. | short sweep | BACKFILL from notes/v2_squiggle_investigation.tex — ACCEPTED safe envelope. Sign-flip metric REJECTED as failure detector (misses band-energy failures). |
| 2026-05-03 | --:-- | v2 Tanaka generator dt sweep (0.01 → 0.32) | v2 uses `L=2π` (v1 was `L=164`), so wave periods and CFL forced a conservative `dt=0.01` default that projected to ~32 days for 10M samples. Test whether GL2-IF is over-resolved and a larger dt is safe. | 4 representative ICs, `tmax=50, B=4`, compared end-state `η(x, t=50)` to `dt=0.01` reference. `dt=0.02→10⁻¹¹` relL2, `0.04→10⁻¹⁰`, `0.08→10⁻⁹` (all machine-eps), `0.16` jumped 8 orders to `6.2e-2` (accuracy fail), `0.32` same `6.2e-2` (wrong FP fixed point). Wall: `dt=0.01` 3098s → `dt=0.08` 402s → `dt=0.32` 105s. All 4 ICs identical band-energy growth in `k∈[80,128)` to within 1% across `dt∈{0.01…0.08}`. | `dt=0.08` bit-exact to `10⁻⁹` relL2 vs `dt=0.01`; period argument (`k=128` mode `T_min≈0.56`, `dt=0.08` = 14% of period). | ~52 min on single H100 | BACKFILL from notes/v2_data_generation_plan.tex — ACCEPTED `dt=0.08` as v2 production default (7.7× speedup, bit-exact); `dt=0.16` REJECTED (6% bias). |
| 2026-05-03 | --:-- | v2 Tanaka generator batch-size sweep (256–1024) | Test whether increasing batch size past 256 improves throughput at `dt=0.08, tmax=200, float64`, or whether H100 HBM bandwidth already saturates. | Single rollout per B at `dt=0.08, tmax=20`, projected to `tmax=200`. `B=256`: 62 min@200, 10.8 GB, `0.557 s/sample`. `B=512`: 123 min, 21.6 GB, `0.554`. `B=768`: 189 min, 32.3 GB, `0.568`. `B=1024`: 249 min, 43.2 GB, `0.561`. Per-step wall `1.48s @ B=256` = ~40% of H100 HBM3 peak; rest is FFT pad overhead + fusion misses. | Per-sample throughput constant across `B∈[256,1024]` → memory-bandwidth saturated at `B≥256`, not launch-limited. | ~7.5h sweep on one H100 | BACKFILL from notes/v2_data_generation_plan.tex — ACCEPTED `B=256` as production (smallest saturating throughput; leaves memory for gxi). |
| 2026-05-03 | --:-- | v2 full-pipeline validation (`keep_samples 4` vs 200) | Verify end-to-end throughput at production `dt=0.08` including IC build + rollout + gxi + NPZ write, and confirm `keep_samples=200` doesn't add material cost. | `B=256, dt=0.08, tmax=200, float64`. Run 1 (`keep=4`): `3729.6s/1024 samples = 3.64 s/sample`. Run 2 (`keep=200`): `3652.1s/51200 samples = 0.0713 s/sample`, matches v1's `0.071`. Output 630 MB (`η+ξ+gξ` fp32) = 12.3 KB/sample. gxi compute at 200 keeps ~1 min (not projected ~10 min). Sign-flip diagnostic on 51200-sample batch: max 34 flips on single `h=0.036` snapshot, 11% of batch carrying 1–4% energy at `k∈[80,128)` as IC structure. | Extrapolates to ~2 days for 5M target on 2 H100s (matches v1's 49h); no NaN; band-energy growth `≤1.03×`. | 61 min/batch (steady state) on one H100 | BACKFILL from notes/v2_data_generation_plan.tex — ACCEPTED as v2 production config (5M target ≈ 49h on 2 GPUs, matching v1). |
| 2026-06-04 | --:-- | DNO+PF (40ep) vs FNO baseline (80ep) — 10-regime median rel-L2 at Tmax | One-step pushforward (`k=1, λ=1, dt=0.01`) forces surrogate to be accurate on states it produces at rollout, breaking covariate-shift compounding of pure single-step training. FNO: 6-block width-128 modes-64 (~13.1M params); SpectralDNO adds a linear-in-ξ `Cᵀ F C` spectral-sandwich residual (~17.0M params). Test whether PF closes the long-rollout gap. | Median final-time rel-L2 η by regime (FNO / DNO+PF, Δ, NaN ICs): tanaka_g0 `0.246 / 0.099` (−60%, 0/1); tanaka_g1 `0.605 / 0.255` (−58%, 0/1); bf_g0 `0.174 / 0.130` (−25%, 0/3); bf_g1 `0.193 / 0.130` (−33%, 1/2); bf_modal `0.245 / 0.127` (−48%, 3/2); linear (analytic truth) `1.170 / 0.341` (−71%, 0/4); stokes_deep `0.0036 / 0.0010` (−72%); stokes_finite `0.0035 / 0.0056` (+60%); random_sea_deep `0.0041 / 0.120` (+2799%); random_sea_finite `0.0042 / 0.091` (+2088%). Big wins on soliton/modal/linear; big regression on already-tiny random-sea. NaN counts rise under DNO+PF in shallow/linear regimes (4/16 linear ICs blow up under PF; 0 under FNO). | 16 ICs per regime, wide CIs; confounded by 40 vs 80 ep and ~30% param delta. | --:-- | BACKFILL from notes/rollout_accuracy_report.tex — MIXED. PF is a real but uneven win; queued matched-compute 80-ep resume for apples-to-apples. Random-sea regression open. |
| 2026-06-04 | --:-- | FNO rollout failure-mode forensics (spectrum + physics descriptors) | Diagnose whether long-rollout error is high-k Gibbs / aliasing / amplitude blowup or coherent in-band drift, to pick the right fix (filter/architecture/loss). | Across 3 worst finite-error ICs (linear, bf_modal, bf_g1): rel-L2 grows linearly-to-slightly-superlinearly, `r²_lin ∈ [0.53, 0.93]`. Per-IC cosine similarity between error spectrum and truth spectrum is 0.57–1.00 (FNO); high-k Gibbs energy fraction `<0.18` in every worst case. NaN blow-ups in bf_g1/bf_modal at `t=81–189` of `Tmax=200` while `|η|_max` stays `0.04–0.12` — numerical divergence after coherent drift, not amplitude blowup. Linear smoking gun: FNO at `t=2` has 99% of error power in sideband modes `k=10–50` while truth has all its power at `k=6` — sideband hallucination from mixing ξ into nonlinear features. Within-regime Spearman ρ of final rel-L2 vs IC descriptors: bandwidth on tanaka_g0/g1 = `+0.76/+0.78` (FNO), `+0.84/+0.85` (DNO+PF); max steepness on random_sea_deep = `+0.63/+0.84`, on stokes_finite = `+0.70/+0.83`; `k_peak` on bf_g0/g1 in `[+0.33, +0.87]`. | Diagnostic on stored trajectories; robust across worst-case sample. | --:-- | BACKFILL from notes/rollout_accuracy_report.tex — ACCEPTED: failure mode is coherent in-band, driven by structural (bandwidth/steepness) not distributional (kh/depth) descriptors. Motivates CS-shaped architecture. |
| 2026-06-04 | --:-- | 3-axis numerical sensitivity sweep on tanaka_g0 case 0 (FNO, T=100) | Rule out obvious physical/numerical explanations (integrator dt, IC amplitude, spatial resolution) for the FNO rollout error before committing to model/loss work. | Sweep with other axes fixed. IC amplitude `α ∈ {0.50, 0.75, 1.00, 1.50, 2.00}` → rel-L2 η at T = `0.0059, 0.0050, 0.0043, 0.0080, 0.0139` (minimum at training `α=1`, factor-3 across 4× amp). Integrator substeps `∈ {1, 2, 4, 8, 16}` → `NaN, NaN, 0.0044, 0.0043, 0.0042` (unchanged past `substeps=4`; NaN at low substeps = GL2 instability, not FNO). Spatial `Nx ∈ {512, 1024, 2048}` → `0.1435, 0.0043, NaN@t=32` (33× worse at 512; blows up at 2048). | 40ep FNO baseline ckpt on tanaka_g0 case 0 (`h=0.139, T=100, out dt=1`). | --:-- | BACKFILL from notes/rollout_accuracy_report.tex — ACCEPTED as ruling out dt/amp artifacts; error is model-side. Extra finding: FNO is resolution-locked to training grid (`Nx=1024`), worth flagging for multi-res eval. |
| 2026-06-04 | --:-- | Training-data audit vs 10 eval regimes | Determine whether the rollout ceiling is a data-coverage problem before pursuing model/loss changes. | mmap'd 1000-snapshot sample of the 73.7 GB `combined_dataset_v3.npz`: no NaN/inf in >10⁶ sampled voxels of η, ξ, or `G·ξ`. Every test regime's IC distribution falls inside training central 90% at the median for amplitude, steepness, dominant wavenumber. Two tails only: max-amp Tanaka nips training p99 in η/ξ amplitude; stokes_finite dominant k sits at upper edge (~26) but still within `modes=64` cutoff. One eval-side issue: 1 of 16 bf_g1 ICs has the truth integrator itself diverge to NaN at `t≈146/200` (pollutes aggregates, not training-data). | Sample-based audit; 10⁶ voxels. | --:-- | BACKFILL from notes/rollout_accuracy_report.tex — ACCEPTED as ruling out data-coverage; fix must come from model or training-loss, not more data. |
| 2026-06-05 | --:-- | Linear-regime truth fix (analytic per-mode Fourier propagation) | The legacy "linear" regime ICs are actually nonlinear shallow-water cases (`η/h` up to 0.30, `kh~0.6`) inherited from MATLAB; the Zakharov truth solver was diverging on some ICs, polluting FNO-vs-truth comparisons. Replace truth with analytic linear-Zakharov flow. | Implemented truth as per-mode propagation with `ω²(k)=g·k·tanh(hk), η̂(k,t)=η̂₀ cos(ωt) + G_lin(k)/ω·ξ̂₀ sin(ωt)` (analogous for ξ). Amplitude and top-k structure preserved to `<1%` over full horizon. All linear-regime comparisons in the report use this analytic truth. | Analytic solution; matches per-mode dispersion exactly. | --:-- | BACKFILL from notes/rollout_accuracy_report.tex — ACCEPTED and merged into `solver/evals/eval_suite.py::truth_rollout_linear_analytic`. |
| 2026-06-05 | --:-- | H-cons SpectralDNO training (Modal, first run) | Zakharov is Hamiltonian; penalize `|H₁−H₀|/(|H₀|+ε)` clipped at `C=10` over one Euler step of the surrogate's own dynamics (`λ_H=1, dt=0.01, s_warm=10k`, linear warmup). Physically motivated soft constraint to reduce rollout energy drift. | Reached epoch 10 with best val_loss `0.00464` (ahead of DNO+PF at same epoch) before Modal-worker preemption. Partial 3-regime eval showed rollout tracking DNO+PF: similar-magnitude random-sea regression, slightly worse Tanaka median, marginally higher divergence rate. No breakthrough at that early ckpt. | Preempted; partial 3-regime eval only. | --:-- | BACKFILL from notes/approaches.tex — INCONCLUSIVE at ep 10; clean 25-ep run from scratch relaunched locally for a fair compute-matched comparison. |
| 2026-06-05 | --:-- | CS-DNO width-512 8-block 256-branch architecture debut (~1.1M params) | Encode `G(η)ξ = G₀ξ + Σₙ Gₙ(η⊗ⁿ, h) ξ` as sum of learned blocks `M_out(D,h)[Φ(η) · M_ξ(D,h) ξ]` with zero-init Φ. Linear-in-ξ + polynomial-in-η by construction; targets bandwidth/steepness structural failure mode. Test whether inductive bias alone (no PF/H-cons) beats prior surrogates. | Smoke checks pass: `y(2ξ)=2y(ξ)` to numerical precision (linearity); `y(2η)−y(0) ≠ 2(y(η)−y(0))` after param perturbation (η-nonlinear); `y=G₀ξ` exactly at init. At epoch 11 of a 40-ep run: best val_loss `0.00199` — already 46% below FNO baseline's all-time-best `0.00369` in ~1/7 the training compute. ~1.1M params (order of magnitude smaller than 13.1M FNO / 17.0M SpectralDNO). | Structural smoke tests pass; val loss well below FNO baseline at 1/7 compute. | --:-- | BACKFILL from notes/approaches.tex — PROMISING at ep 11; run continuing to 40 ep. Load-bearing test of "polynomial-in-η is the missing bias" hypothesis. |
| 2026-06-05 | --:-- | cs_dno_w512b8_l256_v3 baseline (final 40-ep result) | Full training + eval of the v3 CS-DNO baseline vs the FNO baseline. Test whether hard-coded Craig-Sulem structure (P1 ξ-linearity, P3 known dispersion via `G₀`, P4 multiplier×multiplication factorization) beats a generic FNO fed `(η,ξ)` as channels. | Trained cs_dno on `combined_dataset_v3.npz` (5,993,989 snapshots), 80/10/10 split, `width=512, n_blocks=8, latent=256, cs_n_polys=3`, all derivs+Hilbert on, `cs_mult_hidden=128, h_clip_max=5.0`, AdamW lr `2e-4` single cosine, wd `1e-4`, batch 256, **40 epochs**, f32, shard_map data parallel. Sobolev-H¹ spectral relative loss `w(k)=√(1+k²)`. **Best-val = `9.28e-4`** rel-H¹, vs FNO baseline (`fno_w128b6_v3_hclip5`, 13.1M params) at `3.69e-3` on same data/loss/80ep — **4× lower error with 11.6× fewer params at half the epochs** (1,132,800 params: 6.0% η trunk, 46.5% Φ projections, 47.6% multiplier MLPs). Long-rollout f64-harness truth-gated status: bf_g0 0% div / med rel-L² η@t=200 `0.0044`; bf_g1 0% / `0.0067`; bf_modal 6.7% / `0.0055`; tanaka_g0 6.2% / `0.0420`; tanaka_g1 18.8% / `0.0443`; linear shallow-steep sweep 27% / `0.084`. | Same dataset/split/loss as FNO baseline for direct comparison; single-step error on exact solitons `~2e-4` even where rollouts die, isolating shallow-water (`h≤0.1, a/h≥0.15`) failure as coverage-not-architecture. | --:-- | BACKFILL from notes/cs_dno_architecture.tex — ACCEPTED as v3 baseline. Sole systematic weakness: shallow water (`h≲0.1`) at moderate steepness (`a/h≳0.15`) on non-soliton states — targeted by v4 shallow-steep family. |
| 2026-06-09 | --:-- | f64 harness vs f32 harness (v3 CS-DNO baseline) | All architectures (FNO, SpectralDNO, CS-DNO) plateaued at rel-L² ≈ 0.11 at t=200 on Tanaka — hypothesis: f32 rollout harness precision floor, not model error. | Ran identical CS-DNO baseline rollout under f32 vs f64 harness (integrator state + spectral ops f64; net still f32). Analytic order-6 operator oracle through same f32 harness also plateaued at 0.112 (vs model 0.1145) → floor confirmed. Median rel-L²(η) at t=200: tanaka_g0 `0.1145→0.0420` (2.7×), tanaka_g1 `~0.11→0.0443` (2.5×), bf_g0/g1/modal `0.09–0.14→0.0044/0.0067/0.0055` (20–26× improvement). Truth self-convergence under substep-doubling `~3e-9` at `t=200`. | Analytic oracle confirms floor is harness-side; f64 truth self-convergence `3e-9`. | short | BACKFILL from notes/v4_dataset_report.tex — ACCEPTED `eval_suite --f64_harness` as new default; "BF problem" was almost entirely a harness artifact. |
| 2026-06-09 | --:-- | Truth-validity gating audit on v3 baseline | Divergence rates on bf_g1 and linear regimes suspiciously high; hypothesis: some ICs' truth integrator itself is failing (Hamiltonian drift `≫ 1e-3`) and getting counted as model failures. | Audited every IC flagged divergent (final rel > 1 or NaN). Found bf_g1 IC15: truth non-finite at `t≈146`, Hamiltonian drift `3.2e-2` (healthy conserve to `1e-7`), surrogate stays finite. bf_modal IC6: truth drift `3.5e-3`. linear: 5 ICs with drift `>1e-3` (up to 1.6). After gating: tanaka_g0/g1 divergence unchanged 6.2%/18.8%, **bf_g1 6.2%→0%**, bf_modal 6.2%→6.7%, linear **50%→27%**. bf_g0 median `0.0044`, bf_g1 `0.0067`, bf_modal `0.0055`. | Saved-MATLAB cross-check confirmed NOT a per-IC validity signal — decorrelates to O(1) on all chaotic ICs including 0.4% ones. | fast | BACKFILL from notes/v4_dataset_report.tex — ACCEPTED truth-validity gate (`drift > 1e-3` or non-finite → exclude); records `truth_invalid_ics` in summaries. |
| 2026-06-09 | --:-- | Linear-regime steepness diagnosis (v3 baseline) | The "linear" eval regime was suspected linearizable; instead its 16 ICs place all at a single shallow `h=0.038` with amplitude swept to `a₀/h=0.30`. Test whether failures sort by steepness. | Per-IC outcomes sort monotonically by `a₀/h`: `a₀/h ≤ 0.12` clean (rel `0.003–0.105`), transition `0.14–0.16` fails intermittently (one model NaN), `a₀/h ≥ 0.20` fails and 5/16 truth-invalid at these amplitudes (drift `1.5e-3` to 1.6). Surrogate on surviving steep ICs over-amplifies harmonic cascade `1.9–3.3×` truth's spectral power at `2k–4k` of fundamental. Gated median `0.084` (worst on the board). | Failure signature is real shallow-water steepening the model overdrives; truth invalid past `a₀/h ≥ 0.2`. | fast | BACKFILL from notes/v4_dataset_report.tex — RECHARACTERIZED "linear" regime as shallow-steep sweep; motivates v4 shallow-steep wavetrain family. |
| 2026-06-10 | --:-- | v3 single-step accuracy on soliton vs off-manifold states | Diagnose whether the shallow-steep rollout failures are learnable (single-step error there is fine, only rollouts fail) or a coverage gap. | v3 corpus has 2M soliton rows, half below `h=0.05`; "linear" training family stops at `h=0.1`. Single-step error on exact soliton states (including steepest failing rollouts): `~2e-4` (well-learned). Below `h=0.02` on-manifold single-step error is 10–70× elevated. Failing eval regime sits at `h=0.038` with only soliton coverage. | Confirms coverage-gap: soliton single-step fine, near-soliton off-manifold states poorly covered. | short | BACKFILL from notes/v4_dataset_report.tex — ACCEPTED as motivation for v4 shallow-steep wavetrain family (not more solitons). |
| 2026-06-11 | --:-- | v4 shallow-steep wavetrain family smoke validation | Before scaling to 480k samples, verify the new generator (1–5 cosine modes, `h ~ logU[0.02,0.12]`, target `kh ~ U[0.2,1.5]`, `a/h ~ U[0.03,0.25]`, `ka ≤ 0.15`, `tmax=120`) produces valid data. | Smoke batch through `solver/gen_data/generate_shallow_steep_dataset.py` (f64 GL2, `filter_fraction=0.25`, order-6/pad-8 labels with 0.25 lowpass). Stored labels match filtered f64 recompute to median **`9e-8`**. Kept-case energy drift median **`~1e-8`** (3 orders inside truth-health tol `1e-5`). Rejection rate 5–9%, concentrated at nominal `a/h ≳ 0.18`. Evolution naturally steepens states to `max|η|/h ≈ 0.5` with energy conserved; amplitudes sit well inside v3 normalization envelope. | Labels match recompute to `9e-8`; drift 3 orders inside gate; rejects where truth actually fails. | smoke | BACKFILL from notes/v4_dataset_report.tex — ACCEPTED as v4 source-11 family (480k samples in two 240k shards, appended to v3 as `combined_dataset_v4.npz`). |
| 2026-06-16 | --:-- | Tanaka substep-convergence sweep on cs_dno_v5 (80/160/320 inner substeps) | If f32 NaN blow-ups in cs_dno_v5 tanaka rollouts were driven by GL2-IF time-integration error rather than a model-side cascade, refining `inner_dt` from 0.01 to 0.005 to 0.0025 should push `nan_time` steadily later. Test at `outer_dt=0.8, tmax=200, gl2_iters=4` on two failing g0 (cid=5, 11), one failing g1 (cid=1000006), one bounded-but-wrong g1 (cid=1000011). | `notes/diag_tanaka_substeps.json`. tanaka_g0 cid=5 (`h≈0.276`): `nan_time = 42.4 → 43.2 → 42.4` across substeps=80/160/320. cid=11 (`h≈0.234`): `91.2 → 109.6 → 100.8`. tanaka_g1 cid=1000006 (`h≈0.293`): `96.0 → 98.4 → 98.4`. tanaka_g1 cid=1000011 (`h≈0.011`, very shallow): no NaN at any substep, all three finite through `tmax=200`, rel_l2 `≈ 1.4330`. Pattern: NaN time is basically flat with respect to inner_dt (moves `≤1` outer step across 4× refinement) and rel_l2 is noisy, not monotonic. | Self-consistent across finite cases. | `wall_s` per case: 80 ss ≈ 112s, 160 ss ≈ 222s, 320 ss ≈ 441s (linear in substeps). | BACKFILL from notes/diag_tanaka_substeps.json — REJECTED time-integration as the primary NaN driver; cascade is model-side (aligns with later NaN-cascade traces + fp64 retrain motivations). |
| 2026-06-18 | --:-- | v5 Tanaka failure audit — physics + truth-validity | Determine whether the 4 failing Tanaka cases under `eval_suite_f64h` (v5 ckpt) are IC-physics failures, truth-integrator failures, or model failures. Cases: g0 cid5 (`h=0.276, a/h=0.331`, NaN@41.6); g0 cid11 (`0.234, 0.274`, NaN@91.2); g1 cid1000006 (`0.293, 0.305`, NaN@96.8); g1 cid1000011 (`0.011, 0.194`, div@121.6). | Overlaid ICs vs KdV-matched sech² — all consistent with Tanaka exact soliton shape; JCP09 has stable `a/h=0.6` at `M=6`, all failures have `a/h ≤ 0.331`. Max truth energy drift: cid5 `1.6e-7`; cid11 `2.6e-7`; cid1000006 `1.6e-7`; cid1000011 `4.1e-7` — all **3 orders below the `1e-3` validity gate**. `truth_invalid_ics` empty for both g0 and g1. Coverage: only 1.0% of training Tanaka rows sit at `h≥0.23 AND a/h≥0.27`; cid5/cid1000006 have visible periodic-wrap residual `~0.02–0.04` (generator's own safety threshold is `depth_max=0.08` but dataset uses 0.30). | Truth pristine to `~5e-7` across all 4; ICs physically inside stability envelope. | ~fast diagnostic | BACKFILL from notes/v7_tanaka_failure_audit.md — RULED OUT physics-stability and truth-validity hypotheses; failures are model-side. |
| 2026-06-18 | --:-- | v5 Tanaka failure — spectral error attribution | Given truth is pristine and IC is OK, where does the model error live and how does it seed NaN? | Banded L² err ratios `err_high/truth_high` (`k≥30`) for v5: cid5 `t=2.4→1.29, t=4.8→2.25, t=8.0→3.83`; cid11 `1.23/1.57/1.72`; cid1000006 `1.09/1.52/1.62`; cid1000011 stays `0.007–0.013` (very small h has real high-k truth carrier). Err_high crosses truth_high at **`t=1.6–2.4` (integrator steps 2–3)** while err_carrier stays `≥3×` smaller than err_high throughout. Coherent/incoherent decomp: orthogonal (phase/shape) dominates over amplitude bias `3×`. NaN time inversely scales with `a/h` in `{cid5, cid11, cid1000006}`: `41.6/91.2/96.8`. cid1000011 `α_final = −0.996` (reproduces shape flipped sign; rel-L² blowup is normalization artifact — `abs_err ~ 3e-3` vs `|η|_max = 0.0022`). | Mechanism: f32 model `gξ` output has non-zero `k≥30` noise; truth has near-zero carrier there; noise crosses truth floor within 3 steps; `k⁶` CS amplification cascade → NaN. | ~fast diagnostic | BACKFILL from notes/v7_tanaka_failure_audit.md — CONFIRMED mechanism as model-side high-k cascade seeded by f32 rounding floor; motivates fp64 retrain + eval-side `--filter_gxi` as sharp target. |
| 2026-06-18 | --:-- | v7 architecture ablation: `n_blocks=1` vs baseline vs `min_features` (3-ep val loss) | Determine what in CS-DNO block architecture is load-bearing to inform FNO port-back. Baseline is 8 blocks with `η/η²/η³/∂xη/∂²η/|D|^{1/2}η/Hη` spatial feature stack. | 3-epoch retrain each on `combined_dataset_v7_trim` (3.78M rows), batch 256, lr `2e-4`, wd `1e-4`, modes 64, width 512, latent 256, mult_hidden 128, Sobolev-H¹, scale norm, f32, 2× RTX 6000 Ada. Val ep3: baseline (8 blocks, all features) **`0.00701`**; `n_blocks=1` (single block, all features) **`0.00765`** (+9%); `min_features` (8 blocks, features={η} only) **`0.0162`** (+131%). Ckpt bytes: 12.8/2.3/12.9 MB. Wall: 51.8/10.4/51.1 min. | 3 epochs sufficient — v5 baseline traced same `0.0113→0.00701` trajectory as its full 80-epoch retrain's first 3 epochs; ranking stable by ep2. | 51.8/10.4/51.1 min | BACKFILL from notes/v7_experiments_report.tex — ACCEPTED spatial η-feature stack as the load-bearing win; `n_blocks=1` strict-Pareto (0.178× params, 0.20× wall, 1.09× val); depth NOT load-bearing at 8-vs-1; feature stack IS (2.3× loss to strip). Motivates η-feature-augmented FNO retrain and 1-block CS-DNO 40-epoch retrain for rollout validation. Caveat: single-step val only, rollout suite not re-run. |
| ????-??-?? | --:-- | cs_dno ablation arm: baseline (`n_blocks=8, cs_n_polys=3`, all derivs on) | Establish reference point for the CS-DNO minimal-architecture ablation study on v7_trim: what does full-feature 8-block cs_dno reach in 3 epochs so ablation arms are calibrated against a matched-budget target. | Ran cs_dno on v7_trim (3.78M rows) for 3 epochs at batch=256, lr `2e-4`, wd `1e-4`, modes=64, width=512, latent=256, mult_hidden=128, sobolev_k=1, scale norm on 2× RTX 6000 Ada, JAX fp32 shard_map. Result: **val ep1 `0.0113` → ep2 `0.00768` → ep3 `0.00701`**, ckpt 12,836,073 bytes. Matches v5 80-epoch retrain trajectory in first 3 epochs (`0.0115 → 0.00721`), confirming 3-epoch budget is discriminative. Run dir `outputs/ablation_baseline_031004/`. | 3-epoch signal matches historical v5 first-3-epoch trajectory to within 3%; discriminative. | 51.8 min | BACKFILL from notes/cs_dno_ablation_study.md — ACCEPTED as ablation reference. |
| ????-??-?? | --:-- | cs_dno ablation arm: `n_blocks=1` (all features on) | Test whether the 8-block stack is load-bearing or decoration. If most of the win is the η-feature stack + multilinear form and not depth, a single block should approach the baseline at a fraction of the cost. | Same setup as ablation baseline, only `n_blocks=1`. Result: **val ep1 `0.00963` → ep2 `0.00803` → ep3 `0.00765`**, ckpt 2,282,946 bytes (0.178× baseline params). vs baseline: 1.09× val_loss for 0.178× params, 0.20× wall — strict Pareto win. Run dir `outputs/ablation_n_blocks1_040511/`. | Matched-hp comparison, single seed, 3-epoch signal calibrated against baseline. | 10.4 min | BACKFILL from notes/cs_dno_ablation_study.md — ACCEPTED: depth/block-stacking not load-bearing; a single CS block gets within 9% val at 5.6× fewer params and 5× faster. |
| ????-??-?? | --:-- | cs_dno ablation arm: `min_features` (`n_blocks=8, cs_n_polys=1`, all derivs off) | Test the complementary hypothesis: if depth is decorative and the η-feature stack (η, η², η³, ∂η, ∂²η, ½∂η, ℋη) is the real win, killing the features while keeping 8 blocks should regress hard even though param count is ~unchanged. | Same setup, `cs_n_polys=1` and all deriv flags off. Result: **val ep1 `0.0192` → ep2 `0.0168` → ep3 `0.0162`**, ckpt 12,856,693 bytes (~same params, slightly thinner Φ). vs baseline: 2.31× val_loss for ~1.0× params, ~1.0× wall — strict Pareto loss. Reduces the block to a depth-aware Fourier multiplier on ξ (linear-in-ξ DNO with no η coupling). Run dir `outputs/ablation_min_features_041736/`. | Matched-hp comparison; 3-epoch signal calibrated against baseline. | 51.1 min | BACKFILL from notes/cs_dno_ablation_study.md — ACCEPTED as diagnostic: the η-feature stack (polynomials × spectral derivs) is the load-bearing component of CS-DNO, not depth. Suggests FNO retrofit path via η-feature concatenation channel. |

## Notes-with-no-extractable-experiments (backfill scan reported)

- `notes/cs_dno_jcp09_improvement_plan.md` — pure design/roadmap doc; completed tiers already logged in main table.
- `notes/tanaka_h1_design.md` — design doc, no experiments attached.
- `notes/linear_exact_design.md` — design doc.
- `notes/adaptive_sampling.tex` — design + structural smoke only, no numeric error metrics.
- `notes/rollout_stability_approaches.tex` — narrative summary superseded by `rollout_accuracy_report.tex` per-regime numbers.
- `notes/dno_nls_derivation.tex` — pure derivation.
- `notes/rescaling.tex` — pure dimensional analysis.
- `notes/random_sea_generation.tex` — dataset generation procedure + statistics, no hypothesis-and-test structure.
- `notes/family_coverage_v5.json` — dataset composition only.
