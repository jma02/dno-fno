# Experiments — 2026-07-21

| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-21 | 13:46 | C28 epoch-40 exact mode-loss deletion and ten-family evaluation | The mode-balanced loss is correlated with direct L2, had no isolated causal rollout evidence, and at weight 6 contributes a large gradient. Test whether it can be removed while retaining the simpler L2 + localized-tangent + Hadamard objective. | Completed the fresh 40-epoch C27 replica with only `mode_balanced_weight: 6 -> 0`, then evaluated the final checkpoint on the same 64 Tanaka and 256 non-Tanaka cases under the shared soliton-selective guarded protocol. Despite improving base validation L2 (`.00092031 -> .00089679`) and leaving all 320 predictions finite under that protocol, deletion materially worsens rollout tails. Across all 305 truth-valid cases, C27-to-C28 raw median/p95/mean/max change `.002850/.047694/.011408/.534156 -> .003263/.062459/.016560/1.156535`, ratios `1.145/1.310/1.452/2.165`; aligned p95 changes `.026263 -> .027802` (`1.059x`). Raw counts above `.25/.50/.75/1` change `1/1/0/0 -> 3/1/1/1`. The main regressions are Tanaka-g0 case 27 (`.5342 -> 1.1565`) and Tanaka-g1 cases 24 (`.03268 -> .45041`) and 29 (`.00636 -> .35173`); Tanaka-g1 p95 is `4.13x` C27. Benjamin--Feir has no `.25` failures and better aligned p95 (`.842x`) but worse raw p95 (`1.520x`), a population-level pattern consistent with greater alignment-removable error rather than instability. Full decision file: `outputs/c28_l2_mode_off_full_20260720_050017/c27_final_n32_mode_ablation_acceptance.json`. | C28 has 40 ordered finite epoch records, fresh step-zero initialization, epoch 40 as both final and validation-best, exact Hadamard batch accounting, and an exact serialized configuration difference of only the mode coefficient. For all ten families, times, depths, case IDs, truth eta/xi/Gxi, validity masks, IC hashes, and truth-protocol hashes are byte-identical to C27. Direct NPZ scans verify finite predicted eta, xi, and Gxi at every saved frame. The comparison script passes Ruff and `py_compile`; every predeclared acceptance statistic was recomputed from the paired archives. | Training epoch loop `29:58`; cached two-GPU evaluation and comparison `00:58`. | REJECT MODE-LOSS DELETION; RETAIN C27 AND MODE WEIGHT 6 FOR THE CURRENT PAPER MODEL. The term is not required for observed finiteness in the matched guarded benchmark, but it materially suppresses long-horizon translation/phase tails that one-step L2 validation misses. Do not continue the deletion chain from C28. The next isolated loss ablation is tangent-off directly from C27, retaining mode 6 and Hadamard `.01`; test Hadamard deletion only after selecting between those objectives. |
| 2026-07-21 | 14:17 | Mode-loss mathematical reconstruction and manuscript update | C28 shows that deleting the mode term materially worsens phase/translation tails even though it improves base validation L2, so the paper needs an exact mathematical account rather than the vague claim that the loss “balances modes.” | Traced the implemented functional and rewrote the manuscript around its actual interpretation. For each positive resolved mode, `P_k=|q_k|^2+omega_k^2|eta_k|^2` has the form of the squared phase-space radius of the linear oscillator `eta_dot=q`, `q_dot=-omega_k^2 eta`; in the linear limit it satisfies `P_k=2 g0(k;h) H_k^lin`, while training evaluates the formula with the nonlinear reference target as a scale. The loss robustly averages `|qtheta_k-q_k|^2/P_k` over softly reference-active modes. At a fixed state `eta_k=A_k exp(i phi_k)`, its numerator is exactly `(delta A_dot_k)^2+A_k^2(delta phi_dot_k)^2`; for a progressive linear mode, a speed error gives `r_k approximately .5(delta c/c_k)^2`. This explains secular translation without claiming that the loss pins position or enforces equivariance. Added the full implemented activity/floor/cap definition, contrast with carrier-power-weighted L2, current C27 objective, and exact C28 table to `paper/sections/05_hamiltonian_loss.tex` and `paper/sections/06_numerical_results.tex`; updated the abstract/introduction and corrected the regularizer docstring. | Formulae were traced directly against C27 configuration and `mode_balanced_regularizer.py`: forward RFFT, `K=128`, depth clip 5, activity `1e-4`, denominator floor `1e-6`, absolute floor `1e-24`, ratio cap 100, weight 6 with 500-step warmup, and no dispersion sample weighting. Claims explicitly distinguish the exact linear interpretation from nonlinear use as a scale, one-seed 6-versus-0 evidence from coefficient optimality, and guarded C27/C28 finiteness from unguarded model stability. All ten CPU invariant tests pass, Ruff and `git diff --check` pass, and a full BibTeX plus three-pass LaTeX build produces `paper/main.pdf` with no undefined references, overfull boxes, or warnings. | Mathematical/code audit and manuscript rewrite approximately `00:30`; tests and full PDF build `<00:01`. | RETAIN THE MODE TERM AND RENAME IT IN PROSE AS THE ACTIVE-MODE PHASE-SPACE-NORMALIZED COMPLEX LOSS. Attribute to it improved long-horizon modal phase/translation accuracy under the fixed C27 recipe, not NaN suppression. State that it changes optimization geometry using the same supervised targets, is not phase-only, and that weight 6 remains empirical. Use C28—not the confounded C19/C20 comparison—as the causal evidence, conditional on the shared guarded rollout protocol. |
| 2026-07-21 | 14:47 | C29 fresh full-v9 localized-tangent deletion launched | C28 establishes that the active-mode complex loss cannot be deleted without materially worsening the rollout tail. The remaining route to an at-most-three-term paper objective is therefore to test whether the source-restricted localized tangent projection adds anything beyond universal relative L2, universal mode-normalized supervision, and the independent Hadamard identity. | Launched `outputs/c29_l2_tangent_off_full_20260721_144718` from fresh seed-0 initialization on both local RTX 6000 Ada GPUs. The command retains full v9, relative L2, the 1,342,400-parameter `G0+G1+O(eta^2)` architecture, mode weight 6 with 500-step warmup, sparse Hadamard weight `.01` every 16 steps, global batch 1024, learning rate `2e-5`, and 40 epochs; only `translation_tangent_weight: 10 -> 0` changes. A durable handoff will require a complete finite 40-epoch history, exact Hadamard accounting, committed checkpoints, cached byte-identical C27 truth, finite eta/xi/Gxi at every saved frame, and the same predeclared 5% aggregate/BF noninferiority and threshold-count gates before accepting deletion. | Both GPUs were idle before launch. Removed an obsolete July-13 C20 idle watcher that could otherwise have relaunched an already completed experiment. The launch and handoff scripts pass `bash -n`; the paired comparator passes Ruff and `py_compile`; an independent read-only audit confirms all truth-cache paths. After v9 loading, the serialized C27/C29 configuration difference was asserted to be exactly `{translation_tangent_weight: (10,0)}`; replicated state and optimizer counters are zero, the first scheduled Hadamard batch and ordinary batches are finite, and both the training and automatic handoff processes are live and durable. | Dataset loading and first compilation approximately `00:03`; steady training is `2.57` steps/s, projecting approximately `28:00` for the 40 epochs and `01:00` for cached evaluation; actual pending. | RUNNING EXACT TANGENT-OFF ABLATION. If C29 is noninferior, select the three-term L2 + active-mode + Hadamard objective and stop the required deletion sequence. If it fails, retain C27; a Hadamard-off run is then a separate optional route to three terms, not part of this comparison. |
