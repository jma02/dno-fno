# Experiments — 2026-09-05

| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-05 | 01:47 | Functional script cleanup | The scripts retained data-only classes, duplicate batch evaluators, and one-use orchestration layers after the generation pipeline cleanup. Inlining that plumbing should reduce reading overhead without changing numerical work or CLI outputs. | Removed 11 dataclasses and 27 function declarations; retained plain typed records, shared numerical functions, and required JAX/Matplotlib/multiprocessing/Modal callbacks. Merged identical C27 batch evaluators, assembled Modal arguments directly, and flattened analysis and rendering flows while preserving lazy archive loading and publication cleanup. Production scripts fell by 412 lines; including tests, the reduction is 534 lines. Tests now exercise complete render flows instead of removed helper interfaces. | All 14 script unittest cases and 5 translation scientific/output tests pass; repository Pyright reports zero errors/warnings; scripts Ruff, formatting, and diff checks pass. In-memory Git comparisons preserve C27 depth strata, seeded bootstrap summaries and batching, translation JSON/CSV/NPZ outputs, six resolution/noise cases, Modal command arguments, and the comparison renderer's summary plus all 10 image bytes and lazy-load order. All 3 illustration artifacts and all 33 diagnostic artifacts match after normalizing output-directory strings and PDF creation dates. Profiler kernels are AST-identical apart from annotations; CPU timer smoke passes. Independent analysis review found no behavioral or memory regression. No remote training or GPU benchmark was run. | Script suite `00:00` (15.4 s); translation tests `00:00`; no performance benchmark. | MERGE. CLI interfaces and numerical prescriptions are unchanged; one-use internal APIs were intentionally removed. |
| 2026-09-05 | 02:04 | Retire standalone historical scripts | The GPU admission helper has no remaining callers; the fixed profiler, old-v8/C27 GIF comparison, and dated C27 small-amplitude audit are standalone experiment tools outside the current generation/training path. | Removed the four approved scripts (1,627 lines), leaving current launchers, dataset generation/views, paper renderers, and reusable analysis tools intact. Historical experiment records remain unchanged; retired source is available in Git history. | All 14 script tests and 5 translation scientific/output tests pass. Repository Pyright reports zero errors/warnings; scripts Ruff/format, remaining launcher syntax, and diff checks pass. Independent dependency scan found no remaining code, test, or configuration references to removed scripts or their helpers. | Script suite `00:00` (15.1 s); no numerical or performance experiment. | MERGE. Remove standalone tools without changing the supported workflow. |
| 2026-09-05 | 16:46 | Module-level dataset launcher imports | Moving imports to the top should simplify reading, but JAX reads platform settings before backend initialization and Tanaka fixes its dtype at import time. | Moved imports to module scope; retained the float64 Tanaka environment setting before imports and selected JAX precision/platform through config updates after parsing. No bootstrap or helper added. Production decreased by 12 lines; updated tests add 12. | All 14 script tests, repository Pyright, scripts Ruff/format and diff checks pass. Fresh-process smoke starts with conflicting inherited platform/precision settings, confirms import initializes no backend, reaches generation on CPU, and creates float64 arrays. GPU selection is mock-tested without allocating a GPU. Independent import audit agrees with installed JAX source. | Script suite `00:00` (15.7 s); fresh-process checks `00:00`; no numerical/performance benchmark. | MERGE. CPU remains the default and GPU remains opt-in; paper float64 is preserved. |
| 2026-09-05 | 16:56 | Direct family parameter dictionaries | Each sampler already defines an ordered parameter-group dictionary; separate ID tuples duplicate its keys before the launcher builds generation quotas. | Pointed FAMILIES at the existing dictionaries, kept allocation inline, and named the selected dictionary family_parameter_groups. Removed four redundant ID exports and updated tests to iterate dictionaries or name their exact prior fixtures. Production fell by 6 lines and tests by 16. | All 26 affected sampler/generator tests and 2 launcher tests pass. Repository Pyright, scoped Ruff/format, and diff checks pass. Compared 544 allocations against committed HEAD across all families, including remainders: counts, integer types, and key order match exactly. No remaining code references removed exports. | Affected tests `00:00` (22.2 s); launcher tests `00:00`; no numerical/performance benchmark. | MERGE. Sampling distributions, seeds, group order, and requested totals are unchanged. |
| 2026-09-05 | 17:08 | Inline simulation progress counting | Saved and newly generated batches require identical counting and validation; a shared loop should remove the standalone mutating helper without duplicating its body. | Inlined counting into generate_simulations and unified saved/new batch processing; retained replay of every saved batch even after quotas are met, and released loaded rows before the next generation. Production fell from 173 to 151 lines. Compared the working file against Git HEAD entirely in memory, with no source snapshots. | All 20 affected pipeline/storage/generator/launcher tests pass; repository Pyright, scoped Ruff/format, and diff checks pass. All 642 differential cases match exact results/errors, batch reads, generated group schedules, and attempt offsets, including randomized resumes, retries, extra/malformed batches after quota completion, zero targets, and missing output. | Unit suite `00:00` (4.8 s); no numerical/performance benchmark. | MERGE. Remove the helper while preserving generation and resume behavior. |
| 2026-09-05 | 17:37 | Repo-wide on-rails validation cleanup | Internal helpers repeatedly checked shapes, types, settings, and selections already fixed by entry points, constructors, or validated loaders. Removing these checks should reduce reading overhead without changing supported computations. | Reviewed scripts/evaluation, generation, solvers/Tanaka, training/models, and historical scripts. Removed redundant guards and their unused helper/argument scaffolding; retained numerical rejection gates, external artifact validation, no-overwrite/path safeguards, hardware and replica checks. Kept resolved-batch uniqueness after identifying persisted symlink aliases and covered that case. Documented the entrypoint-invariant rule in AGENTS.md. Production fell by 377 lines; tests by 68, for 445 fewer code lines across 27 files. | All 142 unique tests pass: pipeline 26, samplers/generators 57, scripts/evaluation/GL2 30, training 19, translation 5, saved-DNO/dealiasing 3, real Tanaka solves 2. All 1,828 exact in-memory HEAD comparisons pass: time selection/acceptance 1,620, shuffled batching 192, complete rollout payloads 16. Production four-field prefetch and error forwarding pass; an off-path two-field probe exposed an unchanged sentinel-comparison issue, left out of scope. Repository Pyright and Ruff lint pass. Changed-file formatting passes except three files already unformatted in HEAD; full-repo formatting reports 16 pre-existing files, left untouched. No source snapshots, GPU training, or remote jobs. | Pipeline `00:00` (6.4 s); sampler/generator `00:01` (61.5 s); final scripts/evaluation/GL2 `00:00` (21.6 s); comparisons `00:00`; real Tanaka tests `00:00` each (16.1/37.1 s). No performance benchmark. | MERGE. Valid numerical work, distributions, seeded batch order, and artifact protection are preserved; hypothetical internal misuse is no longer treated as a supported input surface. |
