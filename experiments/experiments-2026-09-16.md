| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-16 | 00:39 | Persisted JONSWAP batch finite and structure audit | The completed generation reported 18,432 accepted simulations, but the persisted files still needed an independent full read to rule out nonfinite values, damaged archives and isolated large finite values before dataset assembly. | A read-only CPU Modal worker opened every array in all 50 NPZ files. It found zero NaN/Inf and zero schema or structural errors across 18,700 attempts, 18,432 accepted simulations and 3,686,400 saved rows. Every simulation has 200 frames on a 1,024-point grid; all 27 groups have the requested 682/683 accepted simulations. Global ranges were eta [-0.03133,0.03716], xi [-0.03728,0.03234] and Gxi [-0.11652,0.11526]. The smallest saved water column was positive at 0.007368. | PASS: every numeric value was read and checked; dtypes, shapes, ordered local IDs, frame resets, increasing times, constant positive depths, family, seed and contiguous filenames all passed. Per-simulation maxima have smooth upper tails rather than isolated finite explosions. The NPZ files do not retain internal Hamiltonian or GL2 telemetry, so this audit does not independently replay those generation-time gates. The volume was not modified. | 00:02 CPU-only remote scan, including image build and startup. | ACCEPT the persisted JONSWAP shards for dataset assembly. No regeneration or numerical repair is indicated by the stored data audit. |
| 2026-09-16 | 02:02 | Equal-row four-family paper dataset | Classical shuffled-row training needs comparable coverage from each physical family without duplicating rare rows or discarding the 200-frame trajectories. | Generated 3,686,400 independent static Stokes states and assembled them on Modal with all 3,686,400 rows from each of Tanaka, Benjamin--Feir and the new JONSWAP corpus. The result has 14,745,600 rows: 3,686,400 per family. Splits were drawn independently per family at the simulation level with seed 42 and an 80/10/10 ratio; the audit found 11,796,720 train, 1,474,440 validation and 1,474,440 test rows. | PASS: the assembler checked finite values while copying Stokes, Tanaka and Benjamin--Feir; the preceding full JONSWAP scan checked every stored value. A separate remote audit verified every array shape, exact family counts, simulation counts and split counts. | 00:22 Modal assembly plus 00:00 metadata audit. | ACCEPT `/data/outputs/paper_dataset_full_equal_20260916/arrays` as the training dataset. |
| 2026-09-16 | 02:03 | C27 equal-family full training, two Modal GPUs | Test whether restoring equally rich 200-frame coverage for all four physical families eliminates the rollout failures seen after training on the sparse-JONSWAP dataset. | Launched `c27_tanaka_tangent_full_equal_modal_h100x2_20260916_020332` for exactly 40 epochs with global batch 1,024 under an `H100:2` request; Modal assigned two H200s. It uses the standard 1,342,400-parameter CS-DNO C27 recipe: AdamW at 2e-5, relative-L2 data loss, Tanaka-only translation tangent weight 10, mode-balanced weight 6 and Hadamard weight 1e-2 every 16 batches. | RUNNING: the detached worker accepted the audited 14,745,600-row dataset, verified replicated optimizer counters, compiled on both GPUs and entered epoch 1 with finite losses. After compilation it sustained about 10.4 batches/s. Final loss and rollout correctness remain pending. | 00:10 normalization scan and compilation; training in progress. | KEEP RUNNING and evaluate the same Tanaka rollout set when training finishes. |
| 2026-09-16 | 14:28 | Stop Modal at epoch 35 and resume locally | The two-H200 run was materially more expensive than desired, while its per-epoch checkpoints made the remaining work transferable to the now-free local GPUs. | Stopped the Modal app, recovered the latest epoch-35 and best epoch-34 checkpoints, and fixed the downloader to skip directory entries during recursive traversal. Reassembled the identical 14,745,600-row dataset locally with the same family order, seed-42 simulation splits and saved normalization statistics. Resumed the same run at epoch 36 on both RTX 6000 Ada GPUs with global batch 1,024 and unchanged 40-epoch schedule. | PASS so far: the local audit reproduced every family/simulation/split count and exact checkpoint normalization statistics. Restore reported epoch 35, optimizer/model counters both 403,235 and finite epoch-36 losses; both GPUs reached 100% utilization. Epochs 36--40 remain in progress. | 00:31 migration, local assembly and verified resume; estimated 06:00--07:00 remaining. | KEEP RUNNING LOCALLY. Modal GPU compute is stopped; retain the same run directory for final rollout evaluation. |
