# Experiments — 2026-07-22

| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-22 | 01:50 | C27 saved-\(G\xi\) sign-flip audit and save-path correction | The old Tanaka cleaner used a dead-zoned sign-change count of \(\partial_x[G(\eta)\xi]\), and one current C27 archive appeared to contain 340 flips at \(t=0\). Determine whether this identifies a genuinely corrupt Tanaka condition, supplies a better guard selector, or exposes an evaluation artifact. | CPU-scanned all 320 registered C27 frame-zero conditions using the historical 3% and 5% deadzones, then compared the archived fields with their source rows and a direct order-six, pad-eight recomputation. The only artificial threshold crossing is Tanaka-g0 case 5: the raw saved \(q\) count is `340 -> 2` at 3% (`164 -> 2` at 5%) after the protocol's \(|k|\le128\) projection. Its source \(q\) has 2 flips; archived \(\eta\) is bit-identical to source and \(\xi\) agrees to `3.4e-10` relative error. Filtering reduces archived/source \(q\) discrepancy from `2.48e-3` to `1.10e-6`. The other 14 counts above 40 are finite-depth Stokes wavetrains whose genuine 42--52 extrema survive filtering, so sign flips are not a cross-family validity classifier. Tracing found that `rollout(save_gxi=True)` recomputed and archived the raw series even though the RHS, accepted states, and dataset generator all project to the resolved band. Added the missing low-pass and a batched CPU regression. | A direct recomputation from archived \(\eta,\xi\) reproduces the stale raw \(q\), while the filtered result agrees with source data to at most `3.41e-6` (g0) and `3.00e-6` (g1) relative error. The new regression fails strongly under the old behavior and now agrees to `1e-12`; the saved-\(q\), Zakharov-dealiasing, and soliton-guard CPU tests pass. Existing C27 NPZ files remain stale for \(q\)-based metrics, but the post-hoc saved field never enters the integration, so \(\eta/\xi\) trajectories and terminal-\(\eta\) conclusions are unchanged. | CPU archive scan and focused verification approximately `00:10`. | ACCEPT THE SAVED-\(G\xi\) FILTER FIX. REJECT THE HISTORICAL SIGN-FLIP COUNT AS THE CURRENT GUARD OR A GENERAL ROUGHNESS TEST; retain it only as a documented diagnostic for the old clipped-tail bug. Regenerate or filter cached truth \(q\) before quoting relative-\(q\) metrics. |
| 2026-07-22 | 02:43 | Historical sign-filter versus spectral/refinement transition audit | The 3%-dead-zone, 10-transition rule definitely produced the old 3.71M-row Tanaka archive used by the April FNO runs. Test whether a current spectral or grid-refinement criterion actually contains that rule before replacing or omitting it in the manuscript. | Recovered the exact cleaner history (`4,261,232 -> 3,712,872`, 548,360 sign-rule drops, zero nonfinite drops) and the April checkpoint chain through `tanaka_1_clean_sub500k.npz`. On all 2M corrected nonadaptive Tanaka-v2 rows, 33,054 satisfy the old predicate; the best simple mode-16 \(q\)-tail proxy needs a 99.833% false-positive rate for 100% recall and 88.466% for 99.9% recall. On the first 51,200 rows of each current adaptive source, a derivative-tail-above-80 threshold containing all 1,031/803 sign positives also flags 99.783%/99.393% of sign negatives. An independent fixed-band \(N=1024\) versus \(2N=2048\) reconstruction then tested all 12 exact frame-zero sign drops in historical batch 0, 40 deterministic retained controls, and three current sign-positive ICs. At provisional \(\delta_K>10^{-3}\), it flags `12/12`, `10/40`, and `0/3`; the current defects are only `4.90e-6--7.17e-6`. The rule's historical success has a direct mechanism: because \(q=\eta_t=-c\eta_x\) for a traveling crest, dead-zoned sign changes of \(q_x\) count principal curvature-lobe transitions. All 12 frame-zero rejects have normalized periodic-endpoint mismatch above `.05` (median `.219`, versus `2.50e-6` for 242 retained cases), and per-case rejection fraction is strongly anticorrelated with crest-to-boundary clearance (Spearman `-.701`). Historical retained case 163 has only two flips but \(\delta_K=.463\), showing a clipped/grid-inconsistent tail missed by the old rule. | Fixed a material audit identity-mapping issue before accepting the panel: dedup compacted the saved specification list. Regeneration from seed 42 matches all 254 saved specs exactly and identifies cases 195/238 as absent before the sign filter; they are excluded rather than mislabeled. Reconstructed and archived sign counts agree exactly for all 242 retained batch-0 rows in both float32 and float64; the 12 remaining t=0 absences have counts 12--38 and are exact sign-rule drops because the cleaner reports zero nonfinite drops. Scripts pass Ruff and `py_compile`; JSON parses; both figures were inspected. The complete 548,360-row rejected archive is unavailable, so the `12/12` transition result is a batch-0 result, not exhaustive recall. | Full 2M-row CPU scan `00:04`; adaptive scan `<00:01`; corrected independent-refinement runs and case-identity checks approximately `00:12`; end-to-end investigation approximately `00:45`. | RECORD THE OLD RULE AS REAL, GENERATOR-SPECIFIC CURVATURE SCREENING OF THE CLIPPED-TAIL DEFECT, BUT DO NOT RETAIN IT AS A CURRENT VALIDITY TEST. No useful one-grid spectral threshold supersets it. Advance fixed-band independent refinement as the general resolution audit after an \(N,2N,4N\) calibration; use `sign OR refinement` only for literal backward-audit compatibility. Current v9/C27 prevents the seam defect with periodic oversampled construction and did not inherit the historical sign filter. |
| 2026-07-22 | 03:05 | Historical Tanaka periodization counterfactual and complete batch-0 refinement audit | Boundary clearance and endpoint mismatch correlate strongly with the old sign-filter decisions, but correlation alone does not show that the zero-extended embedding caused the excess sign changes. Hold the rejected crest specifications fixed and change only the embedding to test the proposed mechanism directly. | Rebuilt all 254 reconstructable batch-0 initial states independently at \(N=1024\) and \(2N=2048\). All 12 sign-rejected rows have fixed-band \(q\) defect \(\delta_{341}>10^{-3}\), while 29/242 retained rows also fail, so the sign screen is conservative rather than complete relative to that provisional resolution label. For exactly the 12 rejected crest specifications, replacing zero extension by three-copy periodic interpolation changes every \(C_{0.03}\) count from `12--38` to `2--8`; corrected independent periodic reconstruction changes every defect from `.0101--.465` to `5.79e-6--1.095e-5` (median `.0827 -> 7.03e-6`). Across the complete 254-by-200 batch, per-case rejection fraction versus crest-to-boundary clearance has Spearman \(\rho=-.7006\), \(p=8.4\times10^{-39}\); all 12 frame-zero rejects have clearance at most 6.74. | Regenerated the exact archived seed/specification mapping and excluded the two cases absent before sign cleaning. The complete panel reproduces archived sign counts for retained rows; the periodization arm holds every crest specification fixed. Artifacts: `notes/signflip_refinement_full_batch0_20260722.{json,png}`, `notes/signflip_boundary_mechanism_20260722.png`, `notes/signflip_boundary_bins_20260722.tsv`, and `notes/signflip_periodization_counterfactual_20260722.json`. | Complete 254-case CPU refinement approximately `00:20`; mechanism/counterfactual analysis `<00:02`. | ACCEPT THE MECHANISTIC INTERPRETATION, NOT THE RULE AS A UNIVERSAL FILTER. Report the sign count as a family-specific periodicity/curvature screen that removed outputs corrupted by the historical Tanaka embedding. The final generator replaces detection with tangent-Hermite reconstruction, every intersecting periodic image, and fixed-band projection. |
| 2026-07-22 | 03:30 | Three-grid fixed-band data-quality criterion and matched Tanaka calibration | The historical sign rule detected the clipped-tail defect but is family-specific and incomplete, so it is not strong enough to serve as the journal's primary numerical-validity rule. Replace morphology counting by an independently generated grid-consistency quantity, and test that its decision is stable to both a finer reference and the declared tolerance. | Defined \(\Delta_K\) as the maximum relative \(L^2\) difference among fixed-band order-six DNO coefficient vectors constructed independently at \(N=1024\), \(2N=2048\), and \(4N=4096\). The \(K=341\) matched panel contains all 12 historical frame-zero sign rejects, four interior controls, and the same 12 rejected specifications rebuilt periodically. Clipped constructions have \(\Delta_K=2.33\times10^{-2}\)--\(7.08\times10^{-1}\); interior controls have \(1.46\times10^{-4}\)--\(1.64\times10^{-4}\); corrected periodic counterfactuals have \(5.86\times10^{-6}\)--\(1.10\times10^{-5}\). Decisions are identical at \(\tau=3\times10^{-4},10^{-3},3\times10^{-3}\). A proposed hard contraction gate was rejected: clean historical controls contract only by ratios `.854--.904` but satisfy the absolute label budget, whereas corrected periodic cases contract by `.225--.442`. Reorganized the manuscript so the main data section defines this audit and the exact historical sign-rule record moves to a dedicated appendix. | The script compares normalized fixed physical coefficients rather than interpolating the production-grid construction, holds DNO order/padding/band fixed, and reports all three pairwise defects plus \(H^1\) diagnostics. The 28-case JSON passes invariants; Ruff, `py_compile`, `git diff --check`, and a warning-free LaTeX build pass. Scope is explicit: this is a matched generator audit, not evidence that all 7.63M v9 rows were filtered by \(\Delta_K\). | Pinned 16-core CPU audit approximately `00:03`; criterion analysis, documentation, and manuscript revision approximately `00:20`. | ACCEPT THE THREE-GRID MAXIMUM AS THE PAPER-FACING RESOLUTION AUDIT; KEEP THE SIGN RULE ONLY AS HISTORICAL RECORD. Use \(10^{-3}\) as the declared relative label budget, with the decade-wide sensitivity result. Before calling this a dataset-wide cleaning rule, run the cheap independent state-construction screen once per candidate trajectory and a predeclared DNO panel across every physical family. |
| 2026-07-22 | 18:09 | Retrospective v3-to-v4 low-depth packet intervention audit | The proposed four-map paper dataset removes sparse Fourier packets from training, but the first shallow packet source was introduced for a documented off-manifold gap. Before treating all packet rows as unnecessary, determine whether the saved v3/v4 models show a targeted improvement and identify the confounds. | Compared cs_dno_w512b8_l256_v3_20260605_032608 with cs_dno_w512b8_l256_v4_2gpu_20260611_040500. V4 adds 480,000 low-depth packet rows to 5,993,989 v3 rows. Among the same 11 retrospectively truth-valid shallow-steep cases, finite-prediction terminal relative-\(L^2\) surface median/p95 changed .084099/1.645190 (10 finite, one NaN) -> .010585/.179211 (11 finite); nonfinite-or-unit-error failures changed 3/11 -> 0/11. Architecture, batch 256, learning rate \(2\times10^{-4}\), weight decay \(10^{-4}\), relative \(H^1\) loss, and 40-epoch schedule match. This is targeted positive evidence for the original shallow role, not evidence for the later mid/wide additions. | The two saved linear_trajs.npz archives have exactly equal truth arrays, times, depths, and case IDs; both exclude the same truth-invalid IC indices [4,6,13,14,15] under the archived \(10^{-3}\) drift rule. Metrics were recomputed directly from the trajectories. Important confounds: v4 has 8.008% more optimizer updates at the same epoch count, uses two devices versus one, and changes normalization and the validation distribution; its best validation value is 11.25% higher but is not directly comparable. Artifacts are the v3/v4 configs, best-checkpoint metadata, and eval_suite_f64h/linear_{trajs.npz,summary.json} files in the two run directories. | Saved-array audit less than 00:01; document/evidence review approximately 00:20. | TARGETED POSITIVE EVIDENCE, NOT A CAUSAL OR COMPUTE-MATCHED ABLATION. Keep the shallow packet suite as a frozen development test. First ask whether parameterized shallow TMA reproduces the gain; if not, run equal-case, equal-update, two-seed four-map versus four-map-plus-low-packet training. Do not infer a need for all 1.44M packet rows. |
