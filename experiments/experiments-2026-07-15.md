# Experiments — 2026-07-15

| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-15 | 00:04 | C20/C21 principled translation-tail and tangent-normalization audit | C21's tenfold tangent-weight increase repairs the sole C20 raw error above one but redistributes error into a two-crest case, so determine whether the remaining tail is an integrator/spectral/data defect, insufficient weight, or a mismatch between the auxiliary loss and the reported long-horizon metric before launching another fine-tune. | Scanned all 64 matched C20/C21 Tanaka NPZ trajectories frame by frame and audited the committed tangent/trainer code. C21 improves raw/aligned final error in 41/39 of 64. Its six raw errors above 0.25 are four rigid translations, for which alignment removes 99.1--99.95% of squared error, and two differential multi-crest phase cases. Case 1000024 ends with crest shifts `[+6.21,-0.24]` grid points but individually aligned local shape errors only `[3.1%,0.2%]`; case 1000022 ends at `[+3.98,-1.91,+0.34]` with local errors `[3.7%,1.8%,1.0%]`. Tail high-band energy is 0.83--1.07 times truth, slope 0.93--1.02 times truth, Hamiltonian drift within approximately +/-4.4%, and all arrays are finite, ruling out the former cascade and an abrupt GL2 defect. Derived the missing metric conversion: for small shift, relative eta error is `abs(s)*kappa`, `kappa=norm(eta_x)/norm(eta)`. Across 64 states kappa min/median/p90/max is `0.408/6.32/20.51/33.33` and `corr(log h,log kappa)=-0.962`; exact case-27 error-0.25 shift `0.007566` matches `0.25/kappa=0.00750`. The current per-state loss `ell` minimizes local `delta_c^2/(gh)`, whereas the squared instantaneous relative-eta growth is `gamma^2 = integral(B delta_c^2)/integral(eta^2) = gh*kappa^2*ell`. Its C20 frame-zero correlation with final raw error is `0.737` versus `0.650` for the old loss and `0.647` for a fourth-moment alternative; case 27 ranks first and case 24 seventh under the derived factor. Found no source-count dilution bug, but tangent loss is absent from validation/checkpoint selection and v9 splits random rows rather than trajectories. Full derivation and experiment gates: `notes/principled_translation_error_solution_20260714.md`. | Case IDs, truth frames, horizons, and grids match between C20/C21 archives. Continuous periodic alignment, independent crest tracking, spectral/slope/Hamiltonian checks, exact Fourier-shift bisection, and direct algebra from the committed Gaussian loss agree. The proposed identity uses Gaussian integral preservation and has no learned or case-specific weighting. No training result is claimed from this offline audit. | CPU NPZ/code audit approximately `00:25`. | ACCEPT DIAGNOSIS / IMPLEMENT `gamma^2` AS THE FIRST CONTROLLED ABLATION. Run one low-LR epoch from the same C20 control with architecture/data/H1/modal/Hadamard unchanged, calibrate the new auxiliary to 0.5--1.0% of composite loss (likely coefficient order one; never reuse 100), and include it in validation selection. Start with its mean only; add spatial/sample tail risk only if rigid tails improve while differential cases remain. Do not add hard negatives, stronger damping, more GL2 substeps, full-vector double counting, or a fresh 40-epoch run until this diagnostic continuation passes the fixed 64-case and non-Tanaka gates. |
| 2026-07-15 | 01:09 | C21 completed eight-family acceptance and C20 comparison | C21's weight-100 diagnostic materially improves the Tanaka raw tail, but changing shared parameters could regress Benjamin--Feir transfer, random seas, the shallow nonlinear `linear` family, or Stokes waves. Complete the fixed 32-IC acceptance suite and decide whether C21 is a global improvement rather than a case-27 repair. | All eight non-Tanaka families completed. Every family has zero NaNs and zero counted divergences. On their 241 truth-valid cases, C20-to-C21 aggregate median/p95/mean changes are `0.005366/0.07840/0.01803 -> 0.005424/0.07490/0.01785` (`+1.08%/-4.47%/-0.98%`), and raw counts above `0.25/0.50/0.75/1.00` improve `3/0/0/0 -> 2/0/0/0`. Family median/p95 regressions are limited: bf_modal `+5.58%/+5.13%`, random-sea-deep median `+3.14%` with p95 `-0.52%`, random-sea-finite median `+1.61%` with p95 `-2.70%`, bf_g1 median `+1.32%` with p95 `-2.73%`, and stokes-finite p95 `+9.37%` while its median improves `14.53%`; the other comparisons improve or are essentially flat. BF g0 case 23 changes only `0.36379 -> 0.37364` and remains below the `0.40` gate; BF g0 case 0 drops below `0.25`, and linear case 14 improves `0.29865 -> 0.29075`. Combining the previously completed 64 Tanaka cases with this suite gives 305 truth-valid cases: overall median/p95/mean improve `0.007201/0.16082/0.03464 -> 0.006866/0.15369/0.03205` (`-4.66%/-4.43%/-7.47%`), and threshold counts improve `10/1/1/1 -> 8/1/0/0`. Output: `outputs/c21_tangent_w100_from_c20_20260714_171442/eval_final_guarded_non_tanaka_suite_n32_20260714_232425`. | All ten expected family summaries exist and report matching cached truth. An explicit full-array scan verifies every C21 predicted eta, xi, and Gxi value finite in all 320 trajectories, including truth-invalid references. Metrics use each summary's stored truth-invalid index mask, giving exactly 305 valid cases. GPU sharing stretched the first long pair but cannot change deterministic values; no wall-time speed claim is made. | Shared-GPU eight-family wall `01:03`, from 23:24 to 00:27; CPU comparison/finiteness audit `00:01`. | PROMOTE C21 AS THE BEST AGGREGATE ACCURACY/STABILITY CHECKPOINT, WITH C20 RETAINED AS THE CLEAN PHASE-LOSS CONTROL. C21 preserves 0/320 nonfinite predictions, improves the full valid distribution and removes the sole raw error above 0.75, while its small non-Tanaka regressions remain within the acceptance envelope. It is not the final translation solution: tanaka-g1 case 24 and aligned p95 remain worse. Implement the derived `gamma^2` loss and run its controlled one-epoch ablation from C20, not from C21, so the normalization change is isolated from C21's raw weight increase. |
| 2026-07-15 | 01:24 | C22 `gamma^2` implementation and 1%-v9 coefficient smoke | Before spending a full epoch, verify the new loss identity, gradients, multi-device source aggregation, phase-inclusive validation/checkpoint selection, and its actual scale on real v9 batches rather than transferring C21's numerical weight 100 or relying only on 64 evaluation ICs. | Added `phase_growth_loss = gh*kappa^2*relative_speed_loss` to the localized helper while preserving the old M2 return and a separate `--phase_growth_weight`. Added rigid/differential, gamma, kappa, and multiplier diagnostics; wired both old and new terms through the exact selected-source shard correction; and added both to validation/checkpoint composition. Ran `outputs/c22_phase_growth_smoke_20260715_012406` from C20 best on 1% of v9 for 59 steps, batch 1024, lr `2e-6`, phase weight 1, old tangent weight 0, mode weight 6, and Hadamard 0.01. Train/composite-val/H1-val/modal-val/phase-val were `0.00685841/0.00719591/0.00700528/3.09123e-5/5.15305e-6`. Mean train phase loss was `4.80259e-6`, only 0.0700% of train loss at weight 1; full-v9 calibration therefore selects weight 10 for approximately 0.7%, not the panel-estimated order-one coefficient. Mean rigid/differential terms `4.0468e-7 + 4.3979e-6` sum to the total within logging precision; mean kappa is 7.269 and `gh*kappa^2` multiplier 2.132. | Seven analytic CPU invariants pass, including exact rigid growth, k-squared sensitivity, translation invariance, source gating, differential noncancellation, and finite nonzero gradients for both objectives. Trainer/import pycompile, launcher `bash -n`, and Ruff on new code pass. GPU smoke completed 59/59 finite steps, verified replicated startup counters, observed/expected Hadamard `4/4`, selected exactly 17,468 target rows, wrote best/latest/final checkpoints, and selected on H1+modal+phase validation. | Full startup/data load/compile/checkpoint/smoke approximately `00:03`; optimization plus validation `00:01`. | ACCEPT IMPLEMENTATION AND CALIBRATION; DO NOT EVALUATE THE 1%-DATA CHECKPOINT. Weight 1 is safely subdominant but about 10x too small for the planned diagnostic. Proceed to the controlled full-v9 C20 continuation at phase weight 10, keeping the old tangent term off and every other variable fixed. |
| 2026-07-15 | 01:28 | C22 full-v9 `gamma^2` phase-growth continuation from C20 | The calibrated phase-growth objective converts the same local speed defect into the instantaneous growth rate of the reported relative-eta error and should prioritize narrow/shallow profiles without case IDs or hard negatives. Run the smallest causal test: replace C20's old relative-speed tangent term by this loss while holding architecture, data, H1, modal, and Hadamard settings fixed. | Submitted durable host job 6 for `outputs/c22_phase_growth_w10_from_c20_20260715_012811`: one full v9 epoch from C20 epoch-40 best, reset Adam, lr `2e-6` with 200-step warmup, batch 1024 on GPUs 0/1, old tangent weight 0, calibrated phase-growth weight 10, mode-balanced weight 6, and Hadamard weight 0.01. The coefficient targets approximately 0.7% of the converged objective based on the real-v9 smoke below. `scripts/scheduled_c22_phase_growth_w10_20260715_012811.sh` requires a committed final checkpoint and then automatically runs the matched 32+32 Tanaka guarded/f64 evaluation. Scheduler log: `/tmp/c22_phase_growth_w10_from_c20_20260715_012811.scheduler.log`. | Both launchers pass `bash -n`; focused gamma tests, py_compile, Ruff with the trainer's two pre-existing ignores, and `git diff --check` pass. The two pre-existing low-memory semilinear jobs were left untouched; sharing changes wall time but not deterministic training/evaluation semantics. | Projected shared-GPU training `01:05--01:15`, followed by Tanaka evaluation approximately `00:25--00:35`; actual pipeline `01:17`. | COMPLETED; see the 02:45 result row above. |
| 2026-07-15 | 02:45 | C22 completed `gamma^2` training and 64-Tanaka causal evaluation | Test whether replacing the old depth-normalized mean tangent loss by its derived relative-elevation phase-growth rate, `gamma^2 = gh*kappa^2*ell_tan`, repairs both narrow-wave rigid translation and differential multi-crest phase accumulation while retaining C20's fit and zero-nonfinite behavior. | One full-v9 low-LR epoch from C20 completed with train/composite-val/H1-val/modal-val/phase-val `0.00693979/0.00688939/0.00664738/3.36789e-5/3.99332e-6`. The train phase loss was `4.05880e-6`; weight 10 contributed `4.05880e-5`, exactly 0.585% of train loss. Its rigid/differential pieces were `2.40409e-7 + 3.81839e-6`, so the dataset objective was already 94.1% differential. H1 improves 0.088% from C20 and modal val is 3.64% higher, within the pre-registered 5% gate. On matched guarded/f64 Tanaka rollouts, C22 g0 raw median/p95 are `0.03370/0.20827` and aligned `0.00537/0.03286`; g1 raw median/p95 are `0.04461/0.40356` and aligned `0.01061/0.13773`. Combined raw median/p95 are `0.03445/0.37680`, with counts above `0.25/0.50/0.75/1.00` equal to `6/1/0/0`; aligned median/p95 are `0.00834/0.05644`, with counts `1/0/0/0`. The intended narrow rigid case 27 improves substantially beyond both controls: raw/aligned C20 `1.12696/0.05061`, C21 `0.33642/0.03050`, C22 `0.18047/0.02688`. The differential two-crest case 1000024 improves only slightly from C21 and still fails its gate: `0.63952/0.23662` versus required at most `0.50/0.15`; g1 aligned p95 also fails the `0.107` gate. Output: `outputs/c22_phase_growth_w10_from_c20_20260715_012811/eval_final_soliton_spectral_guard_20260715_023013`. | All 36 logged numeric training metrics are finite. Validation equals H1 + `6*modal + 10*gamma^2` exactly; weighted phase and rigid+differential identities close within `7e-15`; Hadamard expected/observed is `373/373`; modal clipping is zero; best/latest/final checkpoint metadata agree at epoch 1. Truth caches, case IDs, time grids, and truth frames match C20/C21. Every value in all C22 predicted eta, xi, and Gxi arrays is finite (`49,348,608/49,348,608` values). Continuous Fourier alignment reproduces the published C20/C21 controls before comparing C22, and nonfinite predictions would have been counted as failures rather than omitted. | Full data load/training/validation/checkpoints `00:59`; parallel 64-case evaluation `00:15`; CPU finiteness/alignment audit `<00:01`; durable scheduled pipeline `01:17`. | PARTIAL MECHANISM SUCCESS / REJECT AS A REPLACEMENT; KEEP C21 AS THE BEST AGGREGATE CHECKPOINT. The derived normalization strongly repairs exactly the high-kappa rigid-translation case it was designed to prioritize and preserves zero nonfinites, but a mean teacher-forced phase-growth penalty does not resolve differential multi-crest phase. C22 regresses combined raw and aligned medians versus C20/C21 and fails the case-24 and g1 aligned-p95 gates, so do not spend the eight-family acceptance suite on it. The next principled loss, if pursued, must control the spatial/sample tail or per-crest differential phase rather than merely raise the mean `gamma^2` weight. |
| 2026-07-15 | 02:56 | C22 frame-by-frame rigid/differential phase audit | C22 strongly repairs the narrow rigid case 27 but fails the two pre-registered multi-crest gates. Determine whether this is a new GL2/spectral instability, whether the derived loss algebraically omits differential phase, or whether dataset-mean teacher-forced aggregation dilutes rare opposite-sign crest-speed errors. | Continuously aligned every saved frame and independently tracked crests for g0 case 27 and g1 cases 1000024/1000022 across C20/C21/C22. Case 27 C22 raw/aligned/translation correction at final is `0.18047/0.02688/0.892` grid points; one translation removes 97.78% of squared error, and its bounded nonmonotone error confirms repair of rigid speed bias rather than suppression of a blow-up. Case 24 C22 raw/aligned grows smoothly at t=`50/100/150/200` as `0.0250/0.0140`, `0.1671/0.0656`, `0.3810/0.1413`, `0.6395/0.2366`; its two crest corrections grow to `[5.984,-0.224]` grid points while separately aligned local shape errors are only `2.93%/0.19%`. Case 22 grows to `0.4158/0.3617`, with three final crest corrections `[4.255,-2.001,0.435]` and local shape errors `4.05%/1.69%/1.18%`; it is the material aligned regression from C21. For cases 27/24/22, final predicted/truth eta high-k energy ratios are `0.939/0.932/0.925`, their maxima over time are `1.000/1.006/1.000`, Gxi high-band maxima are `0.993/1.013/1.000`, slope ratios are `0.957/0.943/0.914`, and maximum absolute Hamiltonian drifts are `2.58%/4.25%/2.53%`. Thus the remaining errors are secular differential phase with mildly under-energetic spectra, not a cascade. | Case IDs, truth/time grids, and archives match exactly. Continuous integer correlation plus sub-grid Fourier minimization reproduces final published alignments; independent local crest registration separates phase from shape. Every eta/xi/Gxi value is finite, and spectral, slope, and Hamiltonian checks agree on bounded dynamics. The logged loss decomposition itself is 94.08% differential and 5.92% rigid, proving the implementation does not algebraically omit differential phase. | CPU trajectory/alignment/spectral audit `00:09`. | ACCEPT THE MECHANISM; DO NOT INCREASE THE MEAN `gamma^2` WEIGHT. A dataset mean over 1,759,649 selected rows has no rare-tail guarantee and can trade opposite-sign crest-speed errors even though the differential term dominates in aggregate. Before another training run, compute the held-out truth-state distribution/ranking of the local differential phase-growth residual. If cases 22/24 are already in its tail, add a modest universal 90%-CVaR or fourth-moment term on `gamma_diff^2` while holding the total auxiliary budget fixed. If they are not, the remaining cause is off-trajectory covariate shift; then use sparse signed one-substep tubular truth labels rather than another mean-loss reweighting. |
| 2026-07-15 | 03:01 | Held-out frame-zero `gamma_diff^2` rank discriminator | The frame audit leaves two plausible next mechanisms: truth-state tail dilution, which would support a CVaR/fourth-moment differential loss, or off-trajectory covariate shift, which would support tubular one-step data. Use the already saved common initial states to test whether cases 22/24 are actually large under the implemented differential statistic before training either intervention. | Reimplemented the committed Gaussian local projection, energy floor, `gh*kappa^2` multiplier, and rigid/differential decomposition in float64 NumPy and ranked all 64 matched C20 frame-zero Tanaka states. Case 1000024 ranks 7th in both total and differential phase growth (`gamma_diff^2=8.6290e-6`); case 1000022 ranks 9th (`2.5817e-6`), so both are genuine upper-tail truth-state residuals. However, the single-crest case 27 ranks first at `2.2435e-3`, about 260 times case 24, and 97.47% of its implemented phase score is classified as differential even though continuous rollout alignment removes 97.78% of its final squared elevation error with one rigid shift. The same ordering persists after training: C22 differential ranks are 1/7/9 for cases 27/24/22, and their frame-zero differential scores are all slightly larger than C20 despite case 27's much better long rollout. Thus the current soft spatial variance does detect cases 22/24, but it does not cleanly identify inter-crest differential phase: within-crest variation on a narrow single pulse dominates it. | Initial eta/depth/truth-Gxi arrays are bitwise matched across C20/C21/C22; only each checkpoint's saved frame-zero predicted Gxi changes. The NumPy formulas reproduce the previously reported total-score ranks (case 27 first and case 24 seventh), and rigid plus differential equals total per state up to floating-point precision. This is a common-initial-state diagnostic only; it is not an audit of every teacher-forced truth frame. | CPU 64-state rank audit `<00:01`. | REJECT A NAIVE FOURTH MOMENT OR UNNORMALIZED CVAR ON THE CURRENT `gamma_diff^2`; IT WOULD OVERWEIGHT THE ALREADY-REPAIRED NARROW SINGLE-CREST CASE. The truth-state-tail hypothesis is partly supported, so tubular data is not yet forced, but the present rigid/differential label is too spatially local to serve directly as an inter-crest risk statistic. Before training again, formulate and validate a coherent packet/crest-speed statistic (or a capped/sample-balanced differential tail) that ranks cases 22/24 without being dominated by case 27, then audit it over held-out truth trajectories. If no such truth-state statistic predicts the growing aligned error, proceed to signed one-substep tubular labels. |
| 2026-07-15 | 03:05 | Phase-audit statistic provenance correction | Reproducing the new differential ranks revealed that the earlier prose quoted final-profile wave-number statistics beside a frame-zero phase factor and called a log-transformed correlation simply a correlation. Separate the quantities so the mathematical report does not imply a raw-linear predictive relationship that was not observed. | On the 64 common C20 initial truth states, `kappa` min/median/p90/max is `0.481986/6.315115/19.330236/35.295275`, Pearson `corr(log h,log kappa)=-0.970689`, and `gh*kappa^2` min/median/max is `0.062402/2.028866/14.458946`. On the 64 final truth profiles, the corresponding `kappa` values are `0.407598/6.318303/20.509144/33.333933` with Pearson log correlation `-0.962017`; final case-27 `kappa=33.333933` is the correct value for its shift-tolerance calculation. The previously reported old-loss/gamma correlations `0.650/0.737` are specifically Pearson correlations of `log(score)` with `log(final raw eta error)`, exactly `0.650221/0.736957`. The raw-linear correlations are `0.796616/0.794757` and therefore do not improve. | Recomputed directly from the matched C20 NPZ arrays in float64 with the committed Fourier derivative and Gaussian local projection. Initial/final case IDs and truth fields are common across checkpoints; `gh*kappa^2` maximum and the rank ordering reproduce the earlier audit. | CPU recomputation `<00:01`. | CORRECT THE NOTES/REPORT LABELS; NO CHANGE TO THE C22 MODEL DECISION. The derived identity and rank-based motivation remain valid, but its empirical support must be stated as improved log-scale ordering/correlation, not improved raw-linear correlation, and initial versus final profile sensitivities must not be conflated. |
| 2026-07-15 | 04:05 | Manuscript n=256 protocol audit and trajectory-held-out panel construction | A ten-family, 256-IC sweep would tighten distributional and tail estimates, but simply increasing the existing first-N evaluation could give falsely precise manuscript claims if the selected orbits had supplied training snapshots. Audit trajectory identity, split semantics, failure accounting, and cache source records before spending an overnight two-GPU run. | The prepared first-256 launcher was stopped before launch. `combined_dataset_v9` directly includes the Tanaka/BF/random-sea rollout sources, while the trainer splits snapshot rows rather than case IDs; a 200-frame orbit therefore contributes about 160 expected training rows and has probability approximately `0.2^200` of being wholly absent from training. Legacy Stokes rows also contain correlated snapshots whose group IDs were discarded. Implemented a CPU-only builder for ten compact fresh-seed IC panels matching the recorded production distributions, strict evaluator support keyed by panel SHA, explicit all-field/all-frame nonfinite and threshold-failure counts, standard-JSON null handling, physical-distribution grouping of replicate panels, separate predicted-H conservation versus truth-energy error, immediate crash-safe truth caches, and checkpoint/truth-protocol hashes. | Source membership and row-split code were inspected directly. Panel/evaluator metric and cache tests pass `8/8`; Python compilation, launcher syntax, and `git diff --check` pass. All ten 256-case panels are finite, exact-shape/hash-valid, use unique new IDs, and have 2,560 distinct state hashes. The completed manifest records source metadata, seeds/streams, case specifications, hashes, and truth semantics. No GPU evaluation was launched from the leaked archives. | Protocol audit and implementation approximately `00:45`; fresh CPU IC construction `00:25`. | REJECT THE OLD ARCHIVE-FIRST-256 SWEEP FOR MANUSCRIPT CLAIMS. REQUIRE THE NEW TRAJECTORY-DISJOINT PANEL MANIFEST, then run unguarded C21 as the primary learned-operator result. Report the soliton guard only as a separate deployment ablation on the same truth. At `n=256`, zero failures implies only a one-sided 95% per-family upper rate of about `1.16%`; p95 is descriptive but supported by only about 13 upper-tail cases. |
| 2026-07-15 | 16:49 | C21 fresh trajectory-held-out 10-panel evaluation, `n=256` per registered family | Replace the statistically weak and trajectory-leaked 32-case historical suite with 2,560 independently seeded test ICs. This measures the stability/error distribution of the promoted C21 checkpoint without selecting any trajectory whose later frames entered optimization, and gives about 13 observations above each family p95. | Evaluated C21 `final_ckpt` unguarded on the ten SHA-locked panels in `data/manuscript_ic_panels_v1_20260715`, using the f64 harness, batched surrogate, nonlinear order-6/pad-8 truth, GL2-IF with inner `dt=0.01`, and two local RTX 6000 Ada GPUs. A monolithic batch of 256 first reached JAX's default 75% allocation pool and repeatedly failed a required `7.84 GiB` allocation; disabling preallocation exposed the same fragmented result-allocation failure with about `33.3/49.1 GiB` resident. At 04:35 the evaluator and launcher were changed to process every ordered 256-case panel as two internal batches of 128 for both truth and surrogate, concatenate on the IC axis, and record the effective batch size. The fresh batch-128 launch began at 04:36:31 and the wrapper completed at 16:49:41. Tanaka g0/g1 were each `256/256` truth-valid, had terminal-eta failure counts at tau 1 of `3/2`, and conditional-finite final eta median/p95 `0.02486/0.3468` and `0.01888/0.3942`. BF g0/g1/modal were `250/246/244` truth-valid, had tau-1 counts `0/0/1`, and median/p95 `0.01358/0.2154`, `0.01396/0.1498`, and `0.01598/0.2730`. Linear was `241/256` truth-valid with median/p95 `0.001629/0.08030`; random-sea deep/finite were `256/254` with `0.001482/0.01440` and `0.002353/0.03253`; Stokes deep/finite were `256/249` with `0.0003423/0.001384` and `0.001933/0.02328`. These five short panels had zero tau-1 failures. Output: `outputs/c21_tangent_w100_from_c20_20260714_171442/eval_final_heldout_unguarded_n256_20260715_035324`; scheduler log: `/tmp/c21_heldout_unguarded_n256_20260715_035324.scheduler.log`. | The completed aggregate has `2,508/2,560` truth-valid cases. Its 52 invalid references comprise 24 cases nonfinite in every stored truth field and 28 additional energy-drift failures. Three attempted model trajectories were nonfinite, but all correspond to truth-invalid references; model nonfinite count on truth-valid references is `0/2,508` in every panel. Pooled terminal-eta counts at tau `0.25/0.5/0.75/1` are `83/23/11/6`, so the unguarded model has six finite tau-1 tail failures despite zero valid-reference nonfinites. Equal-panel macro mean final eta median/p95 is `0.009501/0.153106`. Validation passed across all ten families: canonical, per-family, and root summaries agree; `n_ics_attempted=256`, `rollout_batch_size=128`, f64/batched/unguarded flags, strict all-field reason dictionaries, all four tau fields, ten independently recomputed panel hashes, and checkpoint hash `232ac8e03099e39ffd75366e20adfdf92dd45d773de32a7aee8e9da23e6c7174` match. Each 22-member archive has the expected `(251,256,1024)` trajectory headers and its time/depth/case IDs, validity masks, nonfinite flags, panel hash, and truth-protocol identity agree with its summary. Exactly 52 expected output files remain, including ten nonempty trajectory archives totaling `13,145,048,041` bytes (`12.242 GiB`); crash-safe truth caches and temporary sidecars are absent. `git diff --check` passes. | The two monolithic allocator canaries failed after about `00:07` each. The successful batch-128 suite took exactly `12:13:10` wall from 04:36:31 to 16:49:41: first short wave `00:25:34`, Tanaka wave `03:56:31`, BF g0/g1 wave `03:55:28`, and final mixed wave `03:55:35`. Summed per-family work was `05:23:05` truth plus `16:06:03` surrogate device time across the two GPUs. The measured result finished about ten minutes ahead of the `12:20` active-wall projection. | COMPLETE PRIMARY MANUSCRIPT EVALUATION; ACCEPT THE MEMORY-BOUNDED BATCH-128 IMPLEMENTATION AND THIS ARCHIVE. The batching change preserved case order, equations, precision, time grid, and failure accounting while resolving only device-memory scheduling. Report zero observed model nonfinites among truth-valid references, truth-invalid references separately, and the finite threshold tails explicitly; do not call the unguarded learned model zero-failure because five Tanaka cases and one BF-modal case exceed terminal eta error 1. Keep any soliton guard result as a separately labeled deployment ablation using the same truth, not as the primary claim. |
| 2026-07-15 | 17:54 | C21 six-tail `G(eta)xi` phase-propagation, teacher-forced, and cross-operator audit | The fresh 2,560-IC evaluation's five Tanaka and one BF-modal finite tau-1 tails visually resemble translation. Test the actual kinematic channel rather than inferring mechanism from terminal profiles: since `eta_t=q=G(eta)xi`, each retained nonzero Fourier mode obeys `phase_dot_k=Im(q_hat_k/eta_hat_k)`. Teacher-forced and exact-DNO-on-predicted-state controls then separate persistent same-state surrogate bias, surrogate error off the truth orbit, and the exact DNO's response to an already displaced state. | Added `scripts/analyze_c21_worst_gxi_phase.py` and CPU-scanned all 251 saved frames of cases Tanaka g0 indices `91/186/224`, g1 `88/202`, and BF-modal `22`. Final raw/aligned eta errors are `1.3262/.03676`, `1.0917/.03469`, `1.1280/.15401`, `1.0261/.02557`, and `1.0396/.02146`; one periodic shift removes `99.923/99.899/98.136/99.938/99.957%` of squared Tanaka error. For every Tanaka mode 1--8, integrating archived on-rollout `Im(q_pred/eta_pred-q_truth/eta_truth)` reproduces terminal phase within `3.8e-4 rad`; an independent continuously aligned `q` budget closes each global shift within `0.02--0.65%`. Teacher-forced C21 `q` error on the five truth orbits remains only about `0.44--1.16%`, has little instantaneous eta-tangent energy, and in three cases predicts the wrong shift sign. The exact split `q_theta(z_pred)-q_*(z_truth) = [q_theta(z_pred)-q_*(z_pred)] + [q_*(z_pred)-q_*(z_truth)]` is decisive: the Tanaka surrogate term stays flat from initial `0.45--1.27%` to final `0.38--1.19%`, while the exact physical state-separation term grows to `3.14--11.75%`. In the aligned phase budget the bounded model term supplies `31.1/-38.1/-13.2/-19.6/6.7%` of the five shifts and the causally downstream exact-state response supplies `69.0/138.1/113.5/119.7/92.6%`. BF case 22 is not rigid: global alignment removes only `41.75%` of squared error, modes `17/20/23` end with displacements `-0.00699/+0.07027/-0.06199` and amplitude ratios `1.495/.305/.968`, and truth/prediction execute different bounded low-mode energy exchange. Its teacher-forced C21 error stays small (`0.663%` final, `6.29%` maximum). At final, the raw total/model-on-predicted-state/exact-state-response `q` errors are `1.3545/.02781/1.3548`; after the eta-optimal shift they are `.9493/.03006/.9502`. Archived on-rollout rates approximately close BF modes 20/23; the exact state response supplies about `86.6%` of mode 20's net rate integral and exceeds the net mode-23 drift while the surrogate term opposes it. | The script passes `py_compile`, Ruff, and `git diff --check`; output is `eval_final_heldout_unguarded_n256_20260715_035324/gxi_phase_audit_tau1`. Recomputing order-6/pad-8 `q_*` on archived truth states matches saved truth `q` to median `3.2--3.6e-6` relative in Tanaka and `8.18e-7` in BF. Initial archived and teacher-forced predictions agree to at most about `1.8e-4` relative in Tanaka and `3.35e-8` absolute in BF. The low-pass identity `eta_error(T)=integral(q_pred-q_truth)dt` closes Tanaka modes through 4 with `1.0--1.7%` residual and through 8 with `4.5--6.1%` residual/cosine above `.99965`. Saved endpoint cadence is `0.8`, so higher-mode trapezoids, especially BF whose carrier exceeds the saved-grid Nyquist, are diagnostic rather than exact GL2 quadrature; BF mode 17 is ill-conditioned and is not used for closure. The late exact-state-response term is causally downstream of earlier model defects, not an independent error source. | Archived CPU analysis about `00:02`; teacher/cross-operator controls ran in parallel in about `00:20` wall. | ACCEPT THE PROPAGATION MECHANISM; REJECT CONSTANT SPEED BIAS, EXPLODING OFF-MANIFOLD SURROGATE ERROR, AND A SIMPLE TUBULAR-DATA FIX. Four single-crest Tanaka tails are almost pure secular translation, the two-crest Tanaka contains a smaller differential-phase component, and BF is bounded modal-transfer/dephasing. In retained modes GL2 propagates the learned `q` vector field; there is no evidence of a spectral cascade or abrupt integrator failure, although a half-step/stage-residual control is the strict numerical exclusion. Small bounded operator defects seed state error, and the correct Hamiltonian/DNO dynamics amplify that state separation into phase. The current eta-only instantaneous tangent projection is not the full coupled finite-time phase-response functional and can have the wrong sign. Do not merely increase C21's weight or add generic off-manifold rows. The principled target is the defect projected onto the full-state adjoint neutral/finite-time phase sensitivity (with modal phase/action sensitivities for BF), or, less selectively, a materially more accurate DNO architecture/full retrain. |
| 2026-07-15 | 19:19 | C23 finite-time full-state GL2 objective implementation and 1%-v9 calibration | The six fresh C21 tails are seeded by small bounded operator defects and then amplified by the correct state-dependent DNO dynamics into rigid/differential phase or BF modal-transfer error. The eta-only instantaneous tangent proxy therefore has the wrong causal horizon and omits the coupled xi response. Test the smallest genuine discrete-adjoint objective before changing architecture: compare learned and order-6 reference paths from the same snapshot under the exact production GL2-IF map, and differentiate through the learned path. | Added `finite_time_phase_regularizer.py` and trainer/launcher wiring. Each active fire takes a source-balanced global microbatch of four from sources `5,6,14,7,8,9`, rolls learned and order-6/pad-8 reference states for eight GL2-IF substeps at `dt=.01` with four Picard iterations, hard `.25` filtering and the zero-mean gauge, then scores every positive mode through `k=128` with the direct full-state Hamiltonian error `g abs(eta_theta_hat-eta_*_hat)^2 + G0 abs(xi_theta_hat-xi_*_hat)^2`, target-only modal normalization and `t^-2` phase/action-growth scaling. Reverse mode through the learned `lax.scan` is the discrete full-state adjoint; reference states are stopped. The 59-step run `outputs/c23_finite_time_phase_smoke_from_c21_20260715_191248` resumed the exact fresh-suite C21 final hash, reset Adam, used interval 4 so all six sources fired `3/3/3/2/2/2`, and completed finite with train/composite-val/H1-val/modal-val `.00705811/.00723909/.00704912/3.16607e-5`. Across 15 fires the raw finite-time loss was `1.02103e-3`; action/phase/polarization growth RMS were `.002776/.010436/.012804`, no scored mode clipped, and every fire selected its full microbatch. Active adjoint batches took about `1.1 s` after compilation versus about `.25 s` ordinarily, materially cheaper than projected. | Six focused CPU invariants pass: exact-path zero, xi-only sensitivity, coherent translation classification, finite nonzero gradient, requested-source sampling, and one-step parity with production GL2-IF. All 14 inherited stage tests pass. `py_compile`, focused Ruff, launcher `bash -n`, and `git diff --check` pass. An independent forced two-CPU-device `shard_map` forward/backward smoke produced finite nonzero loss and gradients. The GPU smoke observed exactly `15/15` finite-time and `4/4` Hadamard fires, wrote all checkpoints, used both devices, and had zero clipped modes. | End-to-end smoke `00:05`, including the 88-GB load, first compilation, 59 updates, validation, and checkpoints; post-compile optimization about `00:00:30`. | ACCEPT IMPLEMENTATION AND CALIBRATION; DO NOT EVALUATE THE 1%-DATA CHECKPOINT. Because sparse terms are not interval-rescaled, an interval-64 active contribution of 0.5--1% would be only 0.008--0.016% epoch-average, while unbiased calibration would create rare 32--64% gradient spikes. The measured cost supports interval 16 instead: run one full-v9 epoch from C21 final at weight `.1`, giving 373 fires/1,492 paired paths, about 1.5% active and 0.09% epoch-average contribution, comparable to the accepted Hadamard budget. Keep old tangent and gamma losses off; use `final_ckpt` and rollout acceptance because this new sparse term is intentionally absent from validation selection. |
| 2026-07-15 | 19:24 | C23 calibrated finite-time full-state continuation from C21 | The real-v9 smoke verifies that the full-state short-horizon discrete-adjoint signal is finite, source dependent, inexpensive enough to sample more densely, and sensitive to both coherent Tanaka phase and BF polarization/modal transfer. Test whether a modest amount of this production-map supervision can reduce C21's six fresh finite tail failures without discarding its already promoted aggregate gains. | Launched `outputs/c23_finite_time_phase_from_c21_20260715_192039` from the exact C21 final checkpoint used by the 2,560-IC audit. Run one full-v9 epoch on both RTX 6000 Ada GPUs, batch 1024, reset Adam, lr `2e-6` with 200-step warmup; retain mode-balanced weight 6 and Hadamard weight `.01`, disable the old eta-only tangent and gamma losses, and use finite-time weight `.1`, interval 16, global microbatch 4, eight GL2-IF substeps at `dt=.01`, Picard 4, filter `.25`, reference order-6/pad-8, `k<=128`, and balanced source cycle `5,6,14,7,8,9`. The 5,964-step epoch schedules 373 fires, 1,492 paired paths, and 62 cycles per source plus one extra source-5 fire. | Reproducible launcher passes `bash -n`; implementation/test evidence is in the 19:19 row. At startup both GPUs were idle, the full dataset loaded normally, the exact C21 final parameters restored, and replicated `state.step=optimizer_count=0` checks passed. The first differentiated compile entered with no allocation or nonfinite error. | Projected full load/compile/train/validation/checkpoint wall `00:35--00:45` from the measured smoke; actual pending. | RUNNING. Accept only after all 5,964 losses and `373/373` finite-time/Hadamard fires are finite, checkpoints commit, H1/modal validation stays controlled, and the pre-registered rollout suite improves the fresh tails without material broad regression. Use C23 `final_ckpt`, not validation-selected `best`, because the new sparse objective is not part of validation composition. |
| 2026-07-15 | 20:07 | C23 calibrated finite-time full-state continuation completed | Determine whether production-map, full-state short-horizon supervision can move the promoted C21 checkpoint while preserving its strong ordinary operator fit; training metrics are a necessary sanity gate before spending on long Hamiltonian rollouts. | `outputs/c23_finite_time_phase_from_c21_20260715_192039` completed its one full-v9 epoch from the exact C21 final checkpoint. Train/composite-val/H1-val/modal-val are `.00690834/.00684408/.00665318/3.18162e-5`. Relative to C21, composite validation improves `0.081%`, modal validation improves `5.52%`, and H1 regresses only `0.085%`. The 373 active fires have finite-time loss/weighted active extra/effective weight `2.01025e-3/1.95374e-4/.098188`; the active term is `2.83%` of mean train loss and approximately `0.177%` after its 1/16 duty cycle. Full-state raw/action/phase/polarization growth RMS are `.024701/.004232/.010325/.015768`, supporting the diagnosis that coupled polarization/modal response is at least as material as coherent phase. | Expected/observed finite-time and Hadamard fires are exactly `373/373`; the source cycle is `63/62/62/62/62/62`, giving 1,492 global paired paths. Every logged scalar is finite, finite-time clipped-mode fraction is zero, and no NaN/Inf/error/firing mismatch appears in the complete log. Best/latest/final checkpoints all exist at epoch 1 with identical train/val metadata; the process exited normally and both GPUs returned to zero utilization. | Train loop `00:40`; validation/checkpoints `00:02`; total trainer runtime `00:45`. | ACCEPT AS A VALID CANDIDATE; ROLLOUT EVALUATION IS THE DECISION POINT. The ordinary fit is preserved and modal validation improves, but these teacher-forced/short-path metrics cannot establish lower long-horizon tails. Evaluate C23 `final_ckpt` first on the fixed 64 Tanaka controls and BF modal transfer, then on the six fresh n=256 failure ICs; promote only if finite/raw/aligned tail counts improve without broad regression. |
| 2026-07-15 | 20:50 | C23 fixed 64-Tanaka rollout and continuous-alignment gate | Test whether the new production-faithful, full-state eight-substep objective improves C21's secular translation/differential-phase tails rather than only its one-step validation metrics. The fixed C20/C21 32+32 Tanaka panel is the pre-registered first rejection gate. | Evaluated C23 `final_ckpt` in `outputs/c23_finite_time_phase_from_c21_20260715_192039/eval_final_soliton_spectral_guard_20260715_200821` with the f64 harness/f32 model, order-6/pad-8 truth, GL2-IF at inner `dt=.01`, four Picard iterations, and filter `.25`. Tanaka g0 raw median/p95 is `.022860/.301875`, with counts above `.25/.50/.75/1` of `3/1/1/1`; g1 is `.052237/.406245`, with counts `3/0/0/0`. Continuous periodic alignment gives g0 median/p95 `.004933/.045051` and g1 `.008690/.096978`. Combined raw median/p95/counts are `.033732/.367509` and `6/1/1/1`; aligned values are `.006661/.057703` and `1/0/0/0`. This is nearly an exact return to the C20 control (`.033030/.358314` raw and `.006689/.057147` aligned), not an improvement over C21 (`.027887/.344140` raw and `.005830/.056214` aligned). The decisive g0 case 27 regresses from C21 raw/aligned `.336422/.030498` to `1.141937/.050135` with a `7.64`-grid-point rigid shift, essentially C20's `1.126959/.050613`; g1 cases 22/24 likewise return to C20-like raw/aligned `.460187/.396699` and `.452921/.143348`. | Both families have `32/32` truth-valid cases and every saved predicted eta, xi, and Gxi value is finite: zero model NaNs/nonfinites. The legacy truth caches failed the stricter protocol hash and were correctly recomputed rather than silently reused. Continuous alignment exhaustively checks circular-correlation peaks and refines them by Fourier translation. Learned-Hamiltonian drift remains bounded (g0/g1 median `.00462/.00658`, p95 `.02191/.02144`), so the lone finite raw error above one is secular translation, not spectral blow-up or GL2 failure. | Parallel two-GPU wall `00:38`; g0 truth/surrogate `00:23/00:15`, g1 `00:22/00:15`; CPU alignment audit `00:00`. | REJECT C23 FOR PROMOTION; RETAIN C21. The finite-time objective was active and numerically correct, but at a `1/16` duty cycle its epoch-average contribution was only approximately `0.177%`; after disabling C21's every-batch weight-100 tangent constraint (`0.683%` of its objective), one ordinary fine-tuning epoch moved the checkpoint back toward C20. Therefore this run does not falsify full-state finite-time supervision in isolation, but it does falsify the tested sparse/short/low-weight term as a replacement for C21's phase constraint. Do not run the expensive broad suite or fresh n=256 gate for this checkpoint. |
