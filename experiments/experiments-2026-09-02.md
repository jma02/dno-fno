# Experiments — 2026-09-02

| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-02 | 15:11 | Rename gen_data Executor→Generator and simplify `trajectory_batch_generator.py` | "Executor" next to the `execution` config was vague; the trajectory executor also duplicated its construct/produce loops per family. | Renamed `trajectory_batch_executor.py`→`trajectory_batch_generator.py`, `stokes_batch_executor.py`→`stokes_batch_generator.py`, `jonswap_horizon_executor.py`→`jonswap_horizon_generator.py` (+ tests) and every `*Executor`/`*_executor` identifier in `scripts/` and `solver/` (`ProcessPoolExecutor` untouched). Merged `_construct_tanaka`/`_construct_jonswap` into `_construct_with_rejections` (BF handled as the no-retry case), merged `_produce`/`_produce_variable_horizons` into one `_produce(initial, grids, family)`, moved the failure remappers to module level with a shared `_remapped_failure_record`, added `_is_strict_index_list` reused by both classifiers, and collapsed `_ordered_outcomes` to a dict fill. `TrajectoryExecutionConfig`/`execution` kept by user choice. | All 165 `solver/gen_data` unittests + 6 `scripts.test_generate_paper_dataset` pass; ruff and pyright clean on touched modules. Module shrank 1506→1363 lines with no behaviour change; `_produce_jonswap` remains the subclass hook used by the horizon-bucketed JONSWAP generator. | Tests ~01:00 on CPU. | MERGE with the in-progress dataset-pipeline simplification. |
| 2026-09-02 | 16:03 | Continue gen_data Executor→Generator refactor | After the first pass, `stokes_batch_generator.py` still returned an `execute` closure and the `BatchGenerator` protocol still said "Execute and save one complete batch", contradicting the new generator vocabulary; the full `solver/gen_data` test and lint state also needed to be reconciled with the in-progress dataset-pipeline simplification. | Renamed the `stokes_batch_generator.py` closure from `execute` to `generate` and updated the `BatchGenerator` protocol docstring to "Generate and save one complete batch." Ran `ruff format` on the 8 affected `solver/gen_data` source/test files, then re-ran the full `solver/gen_data` unittest suite, `ruff check`, `ruff format --check`, targeted `pyright` on the refactored production modules, and `git diff --check`. | All 163 `solver/gen_data` unittests pass; `ruff check` and `ruff format --check` pass for `solver/gen_data`; targeted `pyright` on the changed production modules reports 0 errors; `git diff --check` is clean. The 63 `pyright` diagnostics in the full `solver/gen_data` run are pre-existing test-only typing issues (`JsonScalar`, `NDArray` shape, `assertAlmostEqual` overloads, generic `object` containers) and were not introduced by the generator refactor. | Tests ~01:00; validation ~00:05. | MERGE. Generator terminology is now consistent in `solver/gen_data`; the remaining pre-existing test pyright issues can be addressed separately if needed. |
| 2026-09-02 | 21:33 | Simplified dataset generation and trajectory batching | The worker-era allocation, legacy bitmask records, internal failure-JSON parsers, and test-only execution roles obscured the actual sample/construct/integrate/check/subsample flow and made the current one-run-per-family workflow difficult to review. | Replaced worker-packed attempt records with plain family/split/attempt inputs and accepted-count targets, removed in-progress batch recovery and legacy artifact compatibility, moved trajectory execution settings into `pipeline/trajectory_config.py`, represented construction failures with typed data, and renamed generator stages by their numerical purpose. Added a duplicate-batch-path rejection to the view builder. | Ruff and production Pyright pass. Pipeline, family/generator, and script suites pass 54/54, 100/100, and 25/25; the schema-v2 training-view smoke passes. A real CPU Stokes validation run generated one accepted simulation and its view, then a second invocation resumed without regenerating it. `trajectory_batch_generator.py` is 656 lines, down from 1,292 before the final two passes. | Full tests `00:01`; first CPU smoke `00:00` (2.07 s generation); resume `00:00` (4.6 ms total). | MERGE. The current pipeline retains balanced accepted parameter groups, deterministic retries, numerical checks, one-time trajectory subsampling, atomic batch files, and flat global row shuffling at training time without worker/provenance machinery. |
