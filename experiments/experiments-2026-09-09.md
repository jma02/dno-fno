| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-09 | 00:18 | Retire legacy training and share rollout calculations | The separate baseline trainer, unused loss helpers, repeated family settings, and duplicate prediction/GL2 implementations add maintenance work without helping the current dataset/C27 workflow. | Removed four train-jax files and CARBS; lockfile drops 43 package entries with no retained version changes. Inlined the sole supervised L2 path while preserving precision promotion, shared immutable family settings, simplified prefetch error propagation, and reused fixed GL2 stepping for surrogate rollouts. Compared against `79fa751` in memory on CPU: 94 predictor/solver cases and 108 supervised loss/gradient arrays. Added producer-exception and true-DNO surrogate integration invariants. | PASS; comparisons exact across eager/JIT float32/float64, including gradients, normalization modes, filtering, relaxation, ramping, iteration caps, and nonfinite telemetry. All 42 family settings/types unchanged. Existing solver/evaluation/generation tests and the new GL2 test pass; dataset/prefetch/replication tests pass on CPU, including two devices. Saved-Gxi/dealias tests, CLI help, changed-file Ruff, repo-wide Pyright, and offline lock check pass. No GPU job changes. | 00:04 (183.07 s stepping + 5.45 s predictor + 17.06 s telemetry/casts + 12.61 s supervised loss + 23.22 s solver/evaluation/generation + 11.61 s updated telemetry suite + 1.63 s two-device dataset tests; not a speed benchmark) | KEEP; 1,326 fewer Python lines including tests and existing comment/help edits, plus 990 fewer lockfile lines. |
| 2026-09-09 | 00:33 | Restrict Hadamard norm to L2/H1 | Only orders 0 and 1 are needed, so a general Sobolev-weight loop adds unnecessary code. | Replaced the loop with explicit weights, restricted API types and CLI choices to 0/1, retained default 1, and added known-mode L2 energy coverage. Compared with `c99c9e4` on CPU: 16 eager/JIT float32/float64 cases covering both orders, fixed/random steps, and nonflat/zero/constant surfaces. | PASS; all 64 weight, energy, loss, and parameter-gradient arrays match exactly, including shapes/dtypes. CLI accepts 0/1 and rejects 2. All six Hadamard tests, Ruff, and repo-wide Pyright pass. No GPU job changes. | 00:00 (11.14 s numerical and parser comparisons; not a speed benchmark) | KEEP; five fewer code/test lines. |
| 2026-09-09 | 00:38 | Inline L2/H1 weights | After restricting the norm to 0/1, the weight helper is a short expression that is clearer at its two uses. | Removed the helper and inlined the weights in the projected energy and random probe calculations. Repeated 16 CPU eager/JIT float32/float64 cases against `c99c9e4`, spanning both orders and fixed/random finite-difference steps. | PASS; all 48 energy, loss, and parameter-gradient arrays match exactly, with unchanged shapes/dtypes. All six Hadamard tests, Ruff, and repo-wide Pyright pass. | 00:00 (11.17 s comparisons; not a speed benchmark) | KEEP; five more lines removed, ten fewer code/test lines for the complete L2/H1 cleanup. |
| 2026-09-09 | 00:47 | Local Hadamard operator | The three model evaluations share fixed model, normalization, depth, and dtype inputs, so a local function avoids argument forwarding and partial setup. | Moved evaluation into compute_hadamard_reg as operator(eta, xi), retaining all three calls. Removed the standalone wrapper and its direct test while keeping the actual loss normalization/DC invariant. Compared with `0252f82`: 16 CPU eager/JIT float32/float64 cases with both norms, fixed/random steps, scaled/shifted normalization, depth dependence, and nonzero model DC. | PASS; all 32 loss/parameter-gradient arrays match exactly with unchanged shapes/dtypes. All six Hadamard tests, Ruff, and repo-wide Pyright pass. | 00:00 (11.97 s comparisons; not a speed benchmark) | KEEP; module 181 to 161 lines, 35 fewer code/test lines overall. |
| 2026-09-09 | 01:10 | Direct batch indexing and Hadamard flag | Batch position belongs to the iteration itself, and a host scalar flag does not need a redundant float conversion. | Used enumerate(train_bar), renamed the flag hadamard_ran, and preserved its default and threshold. Compared actual old/new host-loop ASTs against `3333156` over 22 cases with resumed offsets, ragged batches, disabled/missing flags, and threshold-boundary mismatches. | PASS; indexes, update/RNG-call counts, weighted metrics, epoch means, and all five expected error messages match exactly. Two-CPU replication/remainder tests, Ruff, and repo-wide Pyright pass. | 00:00 (0.024 s host comparisons; not a speed benchmark) | KEEP; one fewer trainer line, no schedule or aggregation changes. |
| 2026-09-09 | 01:28 | Exact Hadamard activity check | GPU-averaged activity should match the scheduled 0/1 exactly; thresholding can hide GPU disagreement. | Removed the threshold and renamed the local value hadamard_activity. Checked the actual host-loop AST against ee84622: six valid cases with resumed steps, ragged batches, and disabled/missing flags; six fractional-activity cases on scheduled and skipped steps. | PASS; valid metric totals and epoch means unchanged, all fractional cases fail before aggregation. Ruff and repo-wide Pyright pass. | 00:00 (0.029 s host checks; not a speed benchmark) | KEEP; no added source lines or helpers. |
| 2026-09-09 | 01:35 | Remove per-batch Hadamard schedule assertion | Startup and epoch counter checks cover the known resume-counter failure; the requested simpler loop does not need a second schedule calculation for each batch. | Removed the host schedule assertion and unused batch index. Compared actual host-loop ASTs with ea11dd1 over 24 cases: scheduled/skipped/disabled Hadamard, resumed steps, full/ragged batches, bad counter increments, and replica/counter disagreement. | PASS; valid loss averages, metric totals, updates, and RNG calls unchanged. Remaining counter guards still reject injected failures. Training-step AST unchanged; two-CPU replication/remainder tests, Ruff, and repo-wide Pyright pass. | 00:00 (0.028 s host comparisons; not a speed benchmark) | KEEP; nine fewer trainer lines, no new helpers. Per-batch schedule assertion intentionally removed. |
| 2026-09-09 | 01:47 | Simplify metric accumulation | Skipped Hadamard batches already return zeros, so the host loop can accumulate them without a separate activity flag or skip branch. | Replaced branching with a single per-metric weight; omit the final Hadamard average when there were no evaluations. Compared actual accumulation/averaging ASTs with 60be12c over six disabled, inactive, mixed, delayed-active, ragged, and all-active profiles. | PASS; final epoch field sets and values identical, including absent inactive Hadamard loss. JSON key order may differ. Train-step, RNG, and counter code unchanged. Two-CPU replication/remainder tests, Ruff, and repo-wide Pyright pass. | 00:00 (0.020 s host comparisons; not a speed benchmark) | KEEP; five fewer trainer lines, no new helpers. |
