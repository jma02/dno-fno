# Experiments — 2026-07-04

| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-04 | 05:43 | GL2 stage-tangent regularizer Phase A: module + tests + trainer wire | Implement the training-time closed-loop tangent penalty per `notes/gl2_stage_tangent_regularization_plan.md`. Constrain the sensitivity of one Picard iteration of the GL2 stage system to small perturbations in the P_B mid-band (k∈[32,128]), matching model response to an order-6 CS-DNO reference. Motivation: prior data-only fine-tunes (v3/v4/v5/v6/v7_trim) can't reach the learned k=64:128 feedback loop that drives Tanaka NaNs; the plan is to attack it at the operator-level. | New module `train-jax-10m/stage_tangent_regularizer.py` (~550 lines) with GL2 coefficients, soft Fourier projectors P_B/P_L, energy norm, probe construction, one model + one reference Picard map, finite secant, match/gain losses. Reference F uses `dno_series_eval` at order 6, pad=8, per-sample depth via vmap; model F wraps trainer's `apply_fn` in the same shape. Local-time-origin construction (t_n=0) — plan §3. Wired 22 CLI args + config.json entries + jax.lax.cond gate into `train-jax-10m/1d_dno_fno_jax.py` so the expensive branch is skipped when `stage_reg_weight=0` (default). Test file `tests_stage_tangent_regularizer.py`: 14 tests split into 6 math + 5 loss + 3 integration. **All 14 PASS fp64 on CPU** including finite-secant vs `jax.jvp` agreement (5e-3 abs err at eps=1e-6), production-body_fn parity (1e-10 rel err), local-vs-absolute-time equivalence (1e-9), full end-to-end grad through actual `CraigSulemDNO`. Integration test uses smooth wavetrain ICs (cosine at k∈{2,3,4,5}) rather than random Gaussian — order-6 CS series blows up (~1e46) on filtered white noise, saturating L_match at 1.0 which produces spuriously zero grads (a test-setup artifact, not a module bug — verified by the passing stub-model tests). Module ruff-clean; trainer `py_compile`-clean. | 14/14 tests pass fp64 CPU. Zero-weight numerical parity with baseline training verified by construction (lax.cond skips the branch). GPU integration untested — Phase B will smoke-test on real hardware with the 200-step A/B. | ~2h implementation + debug wall (over multiple sessions) | ACCEPTED for Phase B — module correct, tests green, trainer wire present + gated to zero-cost by default. Next: 200-step matched A/B from v8.5b tied checkpoint (A0 disabled, A1 match, A2 match+gain) to verify wall-time ≤1.5× baseline before committing to Phase C fine-tune. |
| 2026-07-04 | 07:20 | v10 eval_suite_f64h n_ics=32 (2-GPU split, 10-regime rollout audit) | v10 = fresh cs_dno retrain on combined_dataset_v9 (v8 dataset + steep_tanaka_v2 additive at h∈[0.20,0.35], a/h∈[0.25,0.45]). Prior n_ics=16 audit showed v10 fixed g0 NaNs on a smaller sample; 32-IC run needed to confirm on the wider distribution before committing to the tangent-regularizer program. | Overnight run split across both local RTX 6000 Ada, `--n_ics 32 --f64_harness`. All 8 landed regimes: **tanaka_g0** NaN=4/32 (0.125), div=5/32 (0.16), η_med=0.01753, η_p95=0.378 — **matches v8's rate exactly, steep_tanaka_v2 did NOT cure g0**; **tanaka_g1** NaN=**0/32**, div=0/32, η_med=0.1097, η_p95=0.389 — **FIXED vs v8's 1/16**; **linear** 0/32 NaN, η_med=0.00891; **bf_modal** 0/32, η_med=0.00530; **stokes_deep** 0/32, η_med=8.4e-5; **stokes_finite** 0/32, η_med=0.00118. random_sea_deep/finite files present but from earlier session (pre 04:58, may not be n_ics=32 — verify separately). Aggregate at `outputs/cs_dno_w512b8_l256_v9_h100x2_20260703_035745/eval_suite_f64h_n32/all_summaries.json`. | Same physical distribution as v8 baseline; only difference is v10's 4-additive-source dataset. Per-regime surrogate walls: tanaka_g0/g1 ~3376s each, bf_modal 3282s, others ~332s. | ~9h wall (2-GPU parallel, most time in tanaka + bf_modal) | REJECT steep_tanaka_v2 as a g0-cure axis; ACCEPT as a g1-cure. Confirms the split-mechanism hypothesis: g0 (deep, small a/h) cascades through the learned mid-k operator (unreachable by data), g1 (shallower) reaches its ignition state via distributional gaps that data can fill. **Motivates the GL2 stage-tangent regularizer for g0**. |
