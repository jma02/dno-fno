# Experiments — 2026-08-20

| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-20 | 01:51 | Matched archival Tanaka exploding-versus-stable rollout GIFs | The rejected sharp-filter trials and their stable replacements are much easier to compare when the identical initial conditions are animated together rather than shown as unrelated terminal plots. | Added `scripts/render_tanaka_exploding_vs_stable_gifs.py` and rendered both exact archived severe cases as 2x2 animations: rejected sharp $P_{172}$ evolution on the top row, stable power-36 Hou--Li $K=192$ evolution on the bottom, with $\eta$ and internal $G_6(\eta)\xi$ in columns. The top row freezes at its last finite frame ($t=154.24$ and $151.36$) and is explicitly marked as a numerical failure while the matched stable row continues to $T=200$. Outputs are two 1080x780, 183-frame, 15-fps GIFs plus four keyframes per case and a hashed `summary.json` under `outputs/tanaka_exploding_vs_stable_gifs_20260820/`. | The renderer requires exact case-ID, time-grid, shape, and frame-zero $\eta/G_6(\eta)\xi$ equality between archives. Both stable runs reach the terminal frame; Pillow independently reports 183 frames for each GIF. Ruff and `py_compile` pass. Source NPZ hashes are `5cf567f6...ce60` (sharp) and `961d2970...1970` (Hou--Li); GIF hashes are `2d2a3177...ea98` and `af535d16...d546`. Terminal keyframes were visually inspected. | Smoke `00:08`; full render `01:04`; validation `<00:01`. | SUPERSEDED FOR THE REQUESTED MODEL COMPARISON. These artifacts answer a different numerical-filter question and must not be presented as evidence that the three-auxiliary-loss learned model cured the old surrogate cascade. |
| 2026-08-20 | 08:33 | Literature-aligned four-family paper dataset: final generation, postcompletion, and immutable handoff | The literature review required replacing the inherited mixed dataset with source-authenticated Stokes, corrected Tanaka, Benjamin--Feir, and JONSWAP/TMA populations whose declared supports, case-disjoint splits, rejection accounting, trajectory ownership, and training-only normalization are all auditable before any model is trained. | Completed all four accepted quotas and independently authenticated every source family, cumulative view, final manifest/map, numerical audit, worst-case renderer, JONSWAP order diagnostic, family-case figure, and independent training handoff. The original combined-view supervisor timed out only after committing its authoritative output, so the final view was retried and authenticated in isolation rather than regenerating numerical data. Postcompletion also exposed and fixed two release-path defects: preserving the virtual-environment symlink with `absolute()` instead of `resolve()`, and validating `minimum_water_fraction` as a positive ratio rather than incorrectly bounding it by one. Installed the authenticated family figure, updated the paper/readiness/generation documents, created the immutable 630,059-byte document handoff, and compiled the paper plus both long notes. | Final release: 73,728 accepted from 74,045 attempts, 317 zero-row rejections, and 7,686,144 rows. Family accepted/attempted counts are Stokes 18,432/18,432, Tanaka 18,432/18,432, Benjamin--Feir 18,432/18,455, and JONSWAP/TMA 18,432/18,726. Final combined summary SHA-256 is `32f764cf...4307c`; manifest `dbb76ed1...d734`; trajectory map `f26888ad...9cc8`; training-handoff audit `19b4604e...0831`; immutable document handoff `b21830cf...6053`. Postcompletion status is complete with `release_artifacts_mutated=false`. Focused release tests pass 217/217; ruff, `py_compile`, shell syntax, `git diff --check`, stale-status guard, and direct TeX builds pass. No reported model has been trained on this dataset. | Focused release suite `04:57`; final static, hash, and document checks `<00:15`. | ACCEPT AND FREEZE THE DATASET RELEASE. Numerical data generation has zero remaining cases; subsequent empirical work is the predeclared nested learning curve and evaluation, not dataset completion. |
| 2026-08-20 | 09:35 | Matched old-v8 exploding versus three-auxiliary-loss C27 stable model GIFs | The intended visual comparison is the learned-model stability result: show archived old surrogate rollouts that become nonfinite against the later C27 model trained with the active-mode, localized-tangent, and Hadamard auxiliary terms, holding the initial condition fixed. | Added `scripts/render_v8_exploding_vs_c27_stable_gifs.py` and rendered all three old-v8 nonfinite Tanaka cases as top/bottom 2x2 GIFs: old v8 prediction on top, C27 prediction on bottom, with predicted $\eta$ and $G(\eta)\xi$ in columns. The exact matched cases are g0 IDs 5 and 11, whose v8 trajectories first become nonfinite at $t=139.2$ and $172.8$, and g1 ID 1000006, nonfinite at $t=60.0$; every C27 counterpart remains finite through $T=200$. Hou--Li filtering is not presented as the comparison. Outputs are three 1080x780, 122--123-frame GIFs plus four keyframes per case and a hashed `summary.json` under `outputs/v8_exploding_vs_c27_stable_gifs_20260820/`. | For every pair the renderer requires exact case ID, time grid, depth, and bit-identical frame-zero $\eta,\xi$. C27 final relative-$L^2$ elevation errors are `.008424/.003724/.0004545` for IDs 5/11/1000006. Pillow independently confirms the recorded GIF frame counts and dimensions; Ruff, `py_compile`, `git diff --check`, and visual inspection of the last-finite and terminal keyframes pass. GIF SHA-256 values are `0608a2a5...6d1f`, `1738b79d...db10`, and `b8ddaf79...9469`. The C27 archive used the soliton-selective guard, but the saved-frame audit found no Tanaka frame at its nominal trigger and maximum observed weight only `3.15333e-4`; this caveat is recorded in the summary rather than advertised as the method. | Exact archive audit and smoke `<00:01`; full render and validation `00:02`. | USE FOR THE MODEL-STABILITY COMPARISON. The canonical example is g0 case 5 because its mid-band cascade and terminal GL2 loss of contractivity were independently traced; retain the other two as corroborating matched failures. |
| 2026-08-20 | 14:29 | C27 locked recipe on the authenticated four-family paper dataset | C27 is the last promoted full model: its direct relative-L2 data term plus active-mode, localized-tangent, and sparse Hadamard auxiliaries beat C28 mode deletion and the later C29 tangent deletion evidence. Repeating that locked recipe isolates the effect of replacing legacy v9 with the completed literature-aligned dataset. | Prepared a fresh seed-0, two-GPU, 40-epoch C27 replica using the authenticated schema-v2 manifest and train-only scale statistics. Architecture, batch 1024, learning-rate schedule, weight decay, all three auxiliary weights/schedules, exact Craig--Sulem structure, and every inactive loss remain C27-exact; only dataset-derived fields change. The new handoff intentionally selects one stored time from each accepted case per epoch, balanced at 16,384 cases per family: 65,536 training examples (64 optimizer steps) per epoch and a fixed 4,096-case validation draw. Added guarded launch/queue scripts that refuse overwrite/resume, authenticate release and stats fingerprints, require three stable memory polls (42,000 MiB free on GPU 0 and 46,000 MiB on GPU 1), and compare the completed configuration back to C27. The asymmetric threshold preserves the previously validated C27 plus low-memory external-job margin on GPU 0 while requiring the memory-heavy GPU-1 job to exit. | Independently loaded all 1,315 shards (94,666,557,624 bytes; 7,686,144 stored rows) in `00:12`, reconstructed the 6,832,128-row training split, and wrote the trainer cache. Its extrema/scales and canonical fingerprint `4e8ec969...3b8` exactly match the frozen handoff; epoch-0 sampler counts/hashes are 65,536 / `d03ba254...31f` for training and 4,096 / `3cdf8092...461` for validation. Shell syntax and launch guards are checked before queue admission. Both GPUs were occupied at preparation time, so no competing workload was terminated or modified. | Cache materialization `00:13`; projected admitted wall `00:30--00:45` dedicated or approximately `00:45--01:00` with GPU-0 contention; queue delay depends on the existing GPU-1 job. | QUEUE THE FRESH LOCKED-RECIPE RUN. Treat this as a data replacement, not a continuation checkpoint. Forty schema-v2 epochs are a literal C27 schedule but only 2,560 optimizer updates versus C27's 238,560 legacy row-level updates; interpret the result accordingly and extend only as a separately logged compute-matched experiment. |
| 2026-08-20 | 16:31 | C27 new-dataset first launch admission race and clean retry | A short memory-only admission window can become stale while the 94.7 GB dataset view is assembled on CPU: another scheduler may claim a GPU before C27 reaches its first compiled device operation. The failed attempt must not be mistaken for training progress or resumed from a partial state. | The guarded queue admitted `c27_paper_dataset_full_20260820_143121` at 15:58:55 after GPU 1 reported 48,510 MiB free for three polls. A separate pair of 50-epoch FM-U-Net jobs started at 16:03:41, during C27's dataset load, and claimed 42,228 MiB on GPU 1. C27 wrote only its configuration, then hit a 26.29 GiB GPU-1 allocation failure at 16:15:54 during compilation; no optimizer step, epoch record, or checkpoint was written. Stopped only the failed C27 Python/uv processes at 16:31 and released their memory; the FM-U-Net jobs were not modified. Prepared a fresh run name and extended admission from 3 to 31 consecutive 30-second checks, requiring a 15-minute stable memory window before launch. | Atomic evidence is the queue transition at 15:57:55/15:58:25/15:58:55, external-job start time 16:03:41, allocator failure at 16:15:54, a lone 5,308-byte `config.json`, and absence of `train_log.jsonl` or checkpoints. The failed process tree is gone and both external jobs remain live. Shell syntax and `git diff --check` pass for the retry scheduler. | Failed attempt occupied the launch path `00:32`, including `00:12` dataset load and stuck rendezvous diagnosis; zero training updates. Retry queue delay depends on a 15-minute stable GPU window, then projected training `00:45--01:00`. | INVALID ZERO-STEP ATTEMPT; DO NOT RESUME OR COMPARE. Preserve it as admission-race evidence and launch `c27_paper_dataset_full_retry1_20260820_163159` fresh only after the longer stable window. |
| 2026-08-20 | 17:11 | C27 authenticated paper dataset on Modal A100-80GB x2 | The local zero-step failure was an admission race with unrelated GPU work, not a C27 or dataset failure. Moving the exact locked recipe to an isolated two-A100 worker should remove that race while providing ample device and host memory for the 94.8 GB schema-v2 view. | Added a manifest-aware Modal uploader and a dedicated exact-C27 launcher. Uploaded only the manifest, trajectory map, authenticated stats cache, and 1,315 referenced immutable shards: 1,318 files / 94.83 GB in restartable 32-file commits. Validated the `A100-80GB:2` shape under profile `sciml-at-ud`, then launched fresh run `c27_paper_dataset_full_modal_a10080gbx2_20260820_1700` in detached app `ap-z3dwGF4PqdHK00oWqA2P4D`. The worker emitted the expected 40-epoch, batch-1024, seed-0 C27 command with relative L2, active-mode weight 6, localized-tangent weight 10, and Hadamard weight 0.01. Removed only the obsolete local C27 fallback queue after Modal reported one active task. | Local release/stats fingerprints remain the frozen values and the uploader completed without an exception. The worker entered its built-in per-shard/trajectory-map SHA-256 load gate, but the blocking local entrypoint was manually disconnected at 17:16:09 and Modal canceled its input; no epoch, optimizer step, checkpoint, or run artifact was committed. | One-time upload `00:24`; canceled worker lifetime `00:07`; zero training updates. | INVALID ZERO-STEP LIFECYCLE ATTEMPT. Preserve the uploaded dataset, but use only the 17:27 spawned retry for training and evaluation. |
| 2026-08-20 | 17:27 | C27 paper-dataset Modal relaunch with durable spawn semantics | The first Modal worker was numerically untouched but its blocking `Function.remote()` call remained coupled to the local entrypoint: disconnecting that client propagated a cancellation despite the app-level detach flag. Submitting with `Function.spawn()` should return a durable call ID and let the two-A100 worker outlive normal CLI exit. | Added an explicit `--spawn` path to `modal_train.py`, changed the C27 Modal launcher to use it, and submitted fresh run `c27_paper_dataset_full_modal_a10080gbx2_retry1_20260820_1727`. The local command exited normally after returning function call `fc-01M0GH453TCRPG3EANGHK2NDV5`; app `ap-z5AwNubxct0RbNo5cxvMiB` remained detached with one active task and container `ta-01M0GH463J9D2MQZ0ZK36G3MPR`. The intact uploaded dataset volume was reused. | The superseded app stopped at 17:17:02 after a cancellation signal at 17:16:09 and committed no run directory, epoch, checkpoint, or optimizer step. The retry has a new run name, exact C27 command, and remains active after local CLI exit. Ruff (with the two pre-existing ignores), `py_compile`, shell syntax, and `git diff --check` pass. No epoch is claimed while the retry remains in the shard-authentication/load gate. | Relaunch admission `<00:01`; dataset authentication/load in progress. Measure training ETA only after epoch 1. | KEEP RETRY1 ACTIVE. The 17:11 attempt is invalid zero-step lifecycle evidence; use only the spawned retry for training and evaluation. |
| 2026-08-20 | 18:21 | C27 paper-dataset Modal first-three-epoch cadence | The first completed epochs are needed to replace legacy timing guesses with measured new-dataset throughput and to separate GPU training cost from schema-v2 sampling overhead. | The spawned two-A100 retry completed and checkpointed epochs 1--3. Train loss decreased `0.003548 -> 0.002824 -> 0.002463`; validation loss decreased `0.004088 -> 0.002952 -> 0.002481`. Each epoch has 64 optimizer batches and four expected/active Hadamard batches. GPU execution is about 13 seconds per epoch at 4.7--4.8 batches/s, but `build_hierarchical_epoch_indices` currently loops over every eligible trajectory and scans all 7.69 million row labels for each trajectory, producing an approximately eight-minute CPU interval between epochs while both A100s remain allocated at 0% utilization. | Three durable epoch records and latest/best checkpoints exist; all recorded scalars are finite, Hadamard accounting is exactly 4/4, and validation loss is improving. The active task and container remain healthy. The cadence defect is a sampler-complexity/performance issue, not changed data selection or a numerical failure. | Initial dataset authentication/load/JIT about `00:44`; measured steady cadence about `00:08` per epoch despite only `00:00:13` of GPU batches. At 3/40 the unmodified-run ETA is about `04:59`, approximately 23:18 EDT. | CONTINUE CURRENT RUN pending direction; do not misreport the idle interval as GPU training. A bit-exact vectorization of the schema-v2 row lookup would materially reduce cost, but requires a separately verified relaunch decision. |
| 2026-08-20 | 18:52 | Stop C27 paper-dataset Modal run after sampler-cost audit | The measured run showed that nearly all paid two-A100 wall time was being spent in a CPU sampler, so continuing without first checking historical C27 semantics would waste compute and could misrepresent the replica. | Confirmed Modal app `ap-z5AwNubxct0RbNo5cxvMiB` stopped at 18:51 EDT and downloaded its durable log. It completed seven epochs (448 optimizer updates) before stopping; epoch-7 train/validation losses are `.00203178/.00169101`, all logged scalars are finite, and Hadamard accounting is exactly `4/4` in every epoch. Historical reconstruction proves this was not C27's original sampling path: the July-17 C27 config used legacy `combined_dataset_v9.npz` with 6,107,193 training rows, and the trainer returned those row indices and shuffled them directly because the dataset had no `split_id`, yielding 5,964 updates per epoch and 238,560 over 40 epochs. The hierarchical one-time-per-case sampler was added July 29 for schema-v2; its present per-trajectory full-row scan caused the idle-A100 cadence. | The stopped app reports no active worker and the durable volume contains exactly seven ordered epoch records plus checkpoints. The old/new distinction is independently supported by the archived C27 config, trainer branch, experiment record, and Git provenance. The seven-epoch checkpoint is numerically valid but is neither a completed C27 replica nor evidence for a matched sampling protocol. | Spawned worker lifetime approximately `01:24`, including about `00:44` load/authentication and seven short GPU epochs separated by repeated approximately-eight-minute CPU sampling passes. | STOP / DO NOT RESUME AS-IS. Preserve the partial artifacts for audit only. Before spending more GPU time, choose and validate the scientific sampling protocol, then make its row lookup linear-time or pre-indexed and benchmark it without allocated accelerators. |
| 2026-08-20 | 19:19 | Training-script entrypoint consolidation | The repository exposed one canonical trainer through an internal release-bound path, one Modal wrapper at the root, and many experiment launch recipes, making the number of actual training implementations look much larger than it is. A stable two-entrypoint surface should make local and remote training unambiguous without mutating the frozen paper-dataset engine identity. | Added `scripts/train_dno.py` as a thin local wrapper around the canonical JAX engine and moved the Modal entrypoint plus the remaining source-owned Python and shell utilities into `scripts/`. Updated active shell recipes, Modal examples, handoff process matches, and the profiling command to use `scripts/train_dno.py` or `scripts/modal_train.py`; retained `train-jax-10m/1d_dno_fno_jax.py` in place because its source path and digest are part of the frozen release. Added `scripts/README.md` distinguishing the two supported trainers from experiment recipes and legacy implementations. | The wrapper and engine `--help` outputs are byte-identical. `bash -n` passes for every shell file under `scripts/`, and `zsh -n` passes for the moved Tanaka helper; `py_compile` passes for all moved Python entrypoints; Ruff passes for the new wrapper; all four Modal BF contract tests and `git diff --check` pass. No training, dataset, checkpoint, or scientific configuration was executed or changed. | Validation `<00:01`; organization and reference audit `<00:20`. | ACCEPT. The supported training surface is exactly `scripts/train_dno.py` locally and `scripts/modal_train.py` on Modal; launch recipes delegate to these rather than constituting additional trainers. |
