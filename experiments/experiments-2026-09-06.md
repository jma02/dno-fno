# Experiments — 2026-09-06

| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-06 | 00:19 | Windsurf-compatible family request dictionaries | Windsurf reported object-valued iteration for the closed TypedDict union even though repository Pyright accepted it. All request values are integers, so literal-keyed dictionaries can retain the explicit family schemas without relying on closed TypedDict support. | Reproduced the exact multiplication error with the installed Windsurf 1.29.6 language server. Replaced the four schemas by dict types with the same 108 literal keys and int values; removed closed/total and the no-longer-needed direct typing-extensions dependency. Updated the schema-to-sampler invariant test. No generation code, helpers, or runtime conversions were added. | Actual Windsurf diagnostics are clean for the driver, types, and launcher; in-memory probes accept eight valid subset/empty requests and reject thirteen invalid-key, wrong-family, noninteger, or mixed-family requests. All 22 affected tests, repository Pyright/Ruff, changed-file formatting, lock and diff checks pass. Existing local driver docstring/comment edits are preserved outside this commit. | Affected suite `00:00` (4.8 s); real-file Windsurf check `00:00` (1.7 s). No numerical/performance benchmark. | MERGE. Fix integer inference across both checkers without changing sampling, scheduling, quotas, or saved data. |
| 2026-09-06 | 00:43 | Single-source parameter groups | Explicit Literal key lists duplicated every sampler group definition and required a synchronization test plus casts for dynamically allocated quotas. The main launcher already obtains names directly from those dictionaries. | Removed all four mirrored family request aliases and their union, retaining one documented integer-valued request dictionary type. Removed the synchronization test, both casts and unnecessary fixture annotations. Each sampler remains the sole definition of its group names and parameter recipes. Net reduction: 153 code lines, with no new helpers, classes or sampling changes. | All 21 remaining affected tests pass. All 96 in-memory comparisons of actual launcher allocation against Git HEAD match quota values, integer types and key order across four families, three splits, and eight quota sizes. Installed Windsurf reports zero diagnostics for types/driver/launcher, confirms integer iteration/indexing, and rejects noninteger quotas. Repo-wide Pyright/Ruff, changed-file format and diff checks pass; no removed family-alias references remain in Python files. Local driver docstring/comment edits are untouched. | Affected suite `00:00` (5.0 s); allocation comparisons `00:00`. No numerical/performance benchmark. | MERGE. Drop exhaustive static group-name checking as explicitly agreed; retain integer count typing and runtime names from the sampler dictionaries. |
