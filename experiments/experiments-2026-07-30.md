# Experiments — 2026-07-30

| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-30 | 00:40 | Horizon-sorted JONSWAP/TMA production restart | JONSWAP/TMA cases have different 16-peak-period terminal times. The original executor advanced every case in each batch to that batch's longest horizon, so its first 256-case transaction spent only 44.7% of its case-time work on declared horizons. Increasing the unsorted numerical batch would therefore amplify padding rather than improve useful throughput. A larger durable proposal followed by fixed-width groups of similar horizons should preserve the numerical method while removing avoidable integration. | Stopped only the old JONSWAP process and preserved its first `254/256/2` accepted/attempted/rejected commit plus its proposal-only second transaction as superseded audit evidence. Added a JONSWAP-only executor and launcher: each durable proposal contains at most 1,024 cases, indices are stably sorted by `(saved_time_count, proposal_index)`, four numerical groups of at most 256 are passed through the unchanged production executor, and outcomes are restored to proposal order before one atomic commit. The fresh stream-zero proposal has 1,024 cases with saved-count range `272--1359`. Its group ranges are `272--377`, `377--441`, `442--544`, and `545--1359`; sorting reduces the exact four-batch padding proxy from `1,225,728` to `696,576` case-time points (`1.760x`) and raises useful/padded work from 40.18% to 70.70%. The replacement queue is running under `outputs/paper_dataset_jonswap_bucketed_20260730` on GPU 0. | Three focused tests verify stable grouping, proposal-order restoration, inherited quality decisions/rows/time-grid metadata, recursively frozen policy handling, and source identity. All 15 existing trajectory-executor tests, Ruff, `py_compile`, `git diff --check`, a real CLI preflight, and an explicit fail-closed scan against the incompatible old root pass. The frozen first-run fingerprint is `9ed27cd3...e78c`; all 18 shared source hashes remain unchanged and exactly the new launcher and executor are added for JONSWAP. The first proposal records the policy and correct fingerprint. GPU 0 sustained 98--99% utilization during the initial numerical group; GPU 1 and its existing processes were untouched. | Launched `00:40`; first 1,024-case transaction running | RUNNING. Keep the old partial root separate and superseded. Retain the bucketed dataset only after the first atomic commit passes decision, field, hash, quota, and measured wall-time audits; then let the fail-closed queue continue through 16,384 training plus 1,024 validation and 1,024 test cases. |
| 2026-07-30 | 01:33 | Completed Tanaka dataset visual and spectral audit | The production summaries certify completeness, finiteness, water-column positivity, and solved GL2 stages, but those checks do not by themselves show that every accepted trajectory remains spectrally well resolved. Before generating the remaining Tanaka training chunks, inspect representative cases from every category and deliberately search the accepted tail for late-time ringing. | Audited all 4,096 accepted trajectories in the completed 2,048-case train, 1,024-case validation, and 1,024-case test roots, comprising 819,200 stored rows. Rendered one median-roughness test trajectory from each of the eleven direction-aware categories, the six roughest accepted test trajectories, terminal-metric distributions, one 100-frame stress-tail GIF, and a full-dataset comparison of the worst cases against sharp smooth controls under `outputs/tanaka_dataset_confirmation_20260730`. Typical trajectories in all eleven categories remain coherent and translate cleanly. The deliberate tail search nevertheless finds visible late-time grid-scale ringing confined to `steep_m1`: 44/4,096 (`1.07%`) have terminal \(\max|\eta_x|>0.25\), 11 (`0.269%`) exceed `0.5`, and 82 (`2.00%`) jointly have more than 50 dead-zone spatial sign changes in \(G(\eta)\xi\) and absolute \(k\ge64\) target RMS above \(10^{-3}\). The worst train case reaches slope `1.0083`; its ringing begins in \(G(\eta)\xi\) near \(t=112.08\) and in \(\eta\) near \(t=129.84\). | Every stored \(\eta,\xi,G(\eta)\xi,t,h\) value is finite. Minimum stored \(h+\eta\) is `0.00998963`, `0.01000747`, and `0.00997924` in train, validation, and test. Every accepted result has required/evaluated/failed masks `768/791/0`, reports a complete positive-column trajectory with finite state and target, and solved every stage. Maximum accepted residuals are `9.99999911e-9`, `9.99999951e-9`, and `9.99999999e-9`, below \(10^{-8}\). Aggregate accepted category counts are 372--375, with all conditional direction patterns represented. The plotting script passes Ruff, `py_compile`, and `git diff --check`; all rendering was CPU-only. | CPU scan and rendering `00:01` | FAIL DATASET ACCEPTANCE. The visible ringing is unacceptable even though every numerical completion check passes. The \(P_{128}\) projection is active, but the defect grows inside the retained band and the final acceptance policy contains no ringing criterion. Do not generate the remaining 14,336 Tanaka training cases or train on this dataset until a controlled resolution/filter-form diagnosis is complete. |
| 2026-07-30 | 03:04 | Exact Tanaka Hou--Li truth replay against the archived hard-\(P_{128}\) dataset | The completed Tanaka dataset contains a finite late-time ringing tail even though the sharp \(P_{128}\) projection is active. JCP09 applies a smooth multiplier to the surface variables after every time step. Test whether that operation removes both catastrophic and moderate ringing without materially changing clean steep controls. | Fixed a nine-case test panel from immutable test proposals: three catastrophic cases with retained-frame \(D_H>10^{-3}\), three moderate ringing cases with terminal \(\max|\eta_x|=.276\)--`.425` that pass that Hamiltonian gate, and three clean direction/depth controls. `scripts/run_tanaka_houli_filter_replay.py` exactly reconstructs each float64 initial condition and verifies its archived \(t=0\) row under the common \(N_x=1024,M=6,p=8,K=128,\Delta t=.01,T=200\), cap-four GL2 contract. A source audit corrected two important details before the smooth arm: JCP09 equation (29) is \(c_k=\exp[-36(|k|/k_{\max})^{36}]\), so the existing helper needs `m=18`, not its old power-72 default; and the paper filters \(\hat\eta,\hat\xi\) after each step, not the nonlinear stage right-hand side. The diagnostic therefore leaves the production hard \(P_{128}\) RHS projection intact and replaces only the post-GL2 state filter. Initial states and the saved \(q_{\rm ref}=P_{128}G_6(P_{128}\eta)P_{128}\xi\) target remain sharp. | The corrected arm completed all nine cases without a nonfinite value or unsolved GL2 stage. Its maximum stage residual was \(9.999793\times10^{-9}<10^{-8}\); across the panel, maximum relative Hamiltonian drift was \(8.276\times10^{-8}\), maximum slope was `.18060`, maximum common-time \(q_{\rm ref}\) high-band fraction was \(3.153\times10^{-12}\), and every terminal \(q_{\rm ref}\) profile had exactly two cyclic sign changes. Against each case's own archived activity-selected times, the clean controls agree through \(P_{64}\) to relative \(L^2\) errors at most \(1.44\times10^{-8}\) in \(\eta\), \(1.19\times10^{-8}\) in \(\xi\), and \(7.72\times10^{-8}\) in \(q_{\rm ref}\); their full-band errors are at most \(6.75\times10^{-6}\), \(5.54\times10^{-7}\), and \(7.36\times10^{-5}\). For the moderate cases, the hard dataset has per-case maximum slopes `.276`--`.441`, 212--232 \(q_{\rm ref}\) sign changes, and high-band fractions `.009`--`.034`, whereas the smooth replay has slopes `.153`--`.175`, two sign changes, and high-band fractions of order \(10^{-12}\); its maximum \(P_{64}\) relative errors versus the hard archive are `.00128`, `.00134`, and `.00886`. For the catastrophic cases, hard maximum slopes `.718`--`.781`, Hamiltonian drifts `.00353`--`.02045`, and 230 or more sign changes collapse to smooth slopes `.163`--`.181`, Hamiltonian drift below \(10^{-8}\), and two sign changes. Translation alignment barely changes the moderate/severe discrepancy, confirming removal of spectral ringing rather than correction of a phase offset. Ruff, `py_compile`, CPU preflights, deterministic proposal replay, case-specific archive-time gathering, a regression test for distinct per-case selected times, and artifact hashes all pass. The duplicate hard replay was stopped at the user's request after 36 minutes with no artifact; the immutable completed dataset supplied the hard reference. | Duplicate hard arm stopped `03:40` after `00:36`; corrected smooth arm `01:09` end-to-end (`01:07` rollout plus `00:02` sharp target evaluation) on contended GPU 1 | PASS NINE-CASE MECHANISM TEST. The literal state-only, power-36 JCP09 filter removes the retained-band pileup while leaving clean resolved dynamics essentially unchanged. Promote this exact rule to a fresh 256-case Tanaka population smoke, retain the dense Hamiltonian gate, and accept only if rejection rates, invariant drift, low-mode agreement, and category coverage remain controlled. Do not regenerate the full dataset from nine cases alone. The prepared \(K=192\) replay remains an independent mechanism check, not a prerequisite for the population smoke. |
| 2026-07-30 | 03:50 | Dense Tanaka Hamiltonian-drift acceptance gate | The paper-dataset completion checks allow finite, stage-solved trajectories whose retained-band oscillations are already numerically unreliable. The historical steep-Tanaka generator also required relative Hamiltonian drift at most \(10^{-3}\). Reinstate that elementary invariant check on the complete dense trajectory, before activity-based row selection can hide a transient excursion. | Added a Tanaka-only requirement \(\max_j |H_j-H_0|/\max(|H_0|,\mathrm{tiny}_{64})\le10^{-3}\), with \(H_j=\frac{\Delta x}{2}\sum_\ell(\xi_{j\ell}q_{j\ell}+g\eta_{j\ell}^2)\). It is evaluated on all 2,501 float64 saved frames of the production arm before choosing 200 stored rows. A failure retains its attempted-case diagnostics but owns zero shard rows. The threshold is recorded in the execution configuration and therefore its fingerprint; Benjamin--Feir and JONSWAP/TMA remain unchanged. The support annotation now unions its required bit instead of overwriting the Hamiltonian requirement. | Ruff and `py_compile` pass. Independent CPU runs pass all 32 acceptance/writer/executor tests plus all 8 paper-launcher and bucketed-JONSWAP tests. Focused tests establish exact-boundary passage, rejection before the time selector is called, zero-row ownership, paper-Tanaka threshold recording, and preservation of required quality bits. The retrospective scan of the already completed float32/200-frame dataset identifies six \(D_H>10^{-3}\) cases, all in `steep_m1`; this is a lower bound because the new gate sees the dense float64 arm. | Implementation and 40 CPU tests `<00:10` | ACCEPT AS A REQUIRED TANAKA SAFEGUARD, NOT AS THE COMPLETE RINGING CURE. It removes the invariant-breaking catastrophic cases on regeneration, but the retrospective selected-frame scan shows 49 additional terminal-slope cases above `.2` that pass \(D_H\le10^{-3}\). Resolve that broader cutoff-crowding tail with the Hou--Li/buffered-band diagnosis rather than mislabeling this invariant test as a steepness threshold. |
| 2026-07-30 | 17:21 | Paired 256-case Tanaka state-only Hou--Li population smoke | The literal JCP09 state filter removed retained-band ringing in all six moderate/severe sentinels while changing clean \(P_{64}\) dynamics by at most \(O(10^{-8})\). Nine deliberately chosen cases establish the mechanism but do not establish population-wide reliability or rule out rare excessive smoothing. Test the candidate on a balanced population before changing the paper contract or regenerating the dataset. | Applied \(c_k=\exp[-36(|k|/128)^{36}]\) to \(\eta,\xi\) after each completed GL2 step, while leaving the nonlinear stage RHS and stored \(q_{\rm ref}=P_{128}G_6(P_{128}\eta)P_{128}\xi\) sharply projected. Batch zero exactly reused all 256 archived hard-\(P_{128}\) validation specifications. Hou--Li accepted `215/256`; all 41 rejections failed only the dense relative-Hamiltonian-drift gate. Quota fill ended at `256/315/59` accepted/attempted/rejected, again with every rejection solely Hamiltonian drift. The failure was broad over the nine main multicrest/direction cells, whereas both steep one-crest cells passed `23/23`. No trajectory failed finiteness, positive depth, target evaluation, or GL2 stage convergence. The paired CPU audit found that all 183 clean hard-reference cases remained accepted; over exact common retained times their maximum \(P_{64}\) relative errors were \(6.69\times10^{-5}\) in \(\eta\), \(1.91\times10^{-5}\) in \(\xi\), and \(1.28\times10^{-4}\) in \(q_{\rm ref}\). The candidate boundary-band fraction was at most \(9.63\times10^{-5}\), so the filter removes the intended \(96\le |k|\le128\) tail but repeatedly damps physically occupied modes in some main-regime cases. | Exact proposal identities and all hashes verified; final quotas, 51,200 rows, and five committed batches verified. Maximum accepted GL2 residual was \(9.999984\times10^{-9}\), minimum stored water column was `.0140786`, and maximum accepted relative Hamiltonian drift was \(9.403\times10^{-4}\). The final audit reports `numerical_fail` and `blocked_by_batch_zero_failure`. Ruff, `bash -n`, source/fingerprint checks, CPU preflight, 52 focused tests, direct saved-\(q_{\rm ref}\) regression, and the post-run paired comparator pass as software checks; the predeclared numerical acceptance gate fails. | `05:09` wall (`17:21:59`--`22:30:54`); paired CPU audit `<00:01` | REJECT THE GLOBAL EVERY-STEP \(K=128\) HOU--LI CONTRACT. The nine-case mechanism result remains valid, but it was not population-representative: blanket filtering cures steep ringing while creating unacceptable cumulative Hamiltonian loss in about 16% of the paired batch. Keep the current paper contract unchanged and do not regenerate from the quota-filled, acceptance-censored output. If smooth filtering is pursued, place its onset outside the delivered \(P_{128}\) band by evolving on a buffered band, rather than attenuating delivered modes after every step. |
| 2026-07-30 | 23:55 | Paired nine-case fixed-four and exact-v9 Tanaka replay | The global Hou--Li candidate failed because repeated smooth filtering dissipated Hamiltonian, while v9 trained successfully from hard-\(P_{128}\) trajectories. The current paper arm already uses v9's hard projector, so isolate the two remaining evolution differences before changing the dataset: residual-based early freezing of converged GL2 stages and the post-v9 two-grid evaluation of the Zakharov \(\xi_t\) nonlinearity. | Replayed the fixed three-severe/three-moderate/three-clean Hermite panel with exactly four GL2 Picard updates in both arms. Arm A retained the current two-grid \(\xi_t\) evaluation; arm B restored v9's base-grid formula. Both reproduced the archived hard-\(P_{128}\) trajectories closely, but neither changed the ringing mechanism. For the worst severe case, the two-grid/fixed-four and literal-v9 arms respectively gave maximum Hamiltonian drift `.02048` and `.02045`, maximum slope `.7878` and `.7870`, maximum \(96\leq |k|\leq128\) fraction in \(G(\eta)\xi\) `.14013` and `.13983`, and 238 maximum cyclic forward-difference sign changes in both. Across all nine cases the largest \(P_{64}\) relative errors from the archive were \(2.43\times10^{-5},1.73\times10^{-5},4.55\times10^{-5}\) for \(\eta,\xi,G(\eta)\xi\) in the two-grid/fixed-four arm and \(2.96\times10^{-4},1.76\times10^{-4},1.72\times10^{-3}\) in the literal-v9 arm. Terminal overlays are visually indistinguishable and retain the severe oscillatory tail. | Every replay reconstructed the archived initial condition to below \(3.73\times10^{-9}\) absolute error, remained finite with positive water column, and solved every stage below the \(10^{-8}\) residual threshold. All 200 archived times map exactly into both dense replays and include \(T=200\). Four launcher tests and two comparator tests pass; Ruff, `py_compile`, exact proposal checks, and both preflights pass. The comparator was corrected to accept both known replay schemas before producing the paired report. | Two-grid/fixed-four `00:27`; literal v9 `00:23`; comparison `<00:01` | REJECT BOTH EVOLUTION-DIFFERENCE HYPOTHESES AS RINGING CURES. Early stage freezing and doubled-grid evaluation are not the cause of the steep retained-band instability. Do not launch the prepared 256-case literal-v9 population smoke: the nine-case mechanism gate already fails. Keep production stopped and do not restore v9's weak rowwise magnitude cap or sparse 26-frame acceptance check. |
