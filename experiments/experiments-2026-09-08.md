| Date | Time | File / Variant | Motivation | What Tried / Evidence | Correctness | Timing | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-08 | 02:57 | C27 synthetic two-CPU partial-batch training | Fresh simulation splits need not produce row counts divisible by the training batch size or device count. Replicating an indivisible remainder should preserve the averaged objective without padding. | Ran the actual CS trainer with `nx=16`, width 8, one block, latent 4, multiplier hidden 4, batch size 4, one epoch, and all three auxiliaries enabled. Train/validation sizes were 5/7 and 7/5; both runs made two optimizer updates. Three weighted validation losses matched whole-validation references within 2.661e-9 and 1.490e-9. An earlier attempt stalled in Orbax writing and was interrupted at 02:54; successful bounded numerical smokes mocked checkpoint I/O in memory only, without changing production checkpoint code. | All losses finite; exact row coverage. This does not validate real checkpoint writing. Review identified possible excessive GPU memory when a large remainder is replicated. | 00:00 (14.195 s for the successful numerical smoke) | SUPERSEDED by the bounded-remainder implementation below; retain the numerical equivalence evidence. |
| 2026-09-08 | 02:58 | Builder-to-C27 preflight fixture | A launch preflight should accept newly assigned splits and compute its own training-only normalization without requiring historical row totals or scales. | Built synthetic arrays and ran `PREFLIGHT_ONLY=1` through the C27 wrapper. It loaded 32 training, 4 validation, and 4 test rows and checked a finite normalized minibatch without creating a training run. | PASS; array-directory loading and preflight work with fresh simulation splits. | 00:00 (1.752 s) | KEEP the preflight; verify the full paper pool after conversion. |
| 2026-09-08 | 03:02 | C27 bounded-remainder training | Shard the divisible portion of the final batch, replicating only the final fewer-than-device-count rows, to avoid replicating a nearly full batch on every GPU. | Repeated the actual tiny CS trainer smoke on two forced CPU devices with all three auxiliaries active. Five training rows yielded batches 4+1 and two updates; seven yielded 4+2+1 and three updates. Weighted validation losses matched whole-validation references within 3.193e-9 and 3.725e-9. Tangent selected-row totals were exactly 5/7. Persistent regression also covers 1023 rows with batch size 1024, exact coverage, and gradient/loss agreement. Environment: `JAX_PLATFORMS=cpu JAX_ENABLE_X64=True XLA_FLAGS=--xla_force_host_platform_device_count=2 CUDA_VISIBLE_DEVICES=''`. Checkpoint I/O remained mocked only in this numerical smoke. | PASS; finite auxiliaries, no omitted rows, correct optimizer-step counts and row-weighted reporting. Real checkpoint writing and full GPU training remain unvalidated. | 00:00 (17.347 s) | KEEP bounded remainder batches and row-weighted diagnostics; do not launch full training yet. |
| 2026-09-08 | 03:03 | Real Stokes generation → array export → C27 preflight | Public commands should work together on genuine generated fields, not only synthetic fixtures. | Generated 20 Stokes simulations with default seed, batch size 8, and output root `outputs/paper_dataset_workflow_smoke_20260908_0302/generated`; all 20 attempts succeeded, five in each parameter group. Built its single summary into the adjacent `arrays` directory with default split seed 42, then ran `DATASET=outputs/paper_dataset_workflow_smoke_20260908_0302/arrays PREFLIGHT_ONLY=1 bash scripts/launch_c27_paper_dataset_full.sh`. | PASS; 16/2/2 train/validation/test rows and simulations, finite normalized minibatch, no training run launched. | 00:01 (end-to-end including inspection pauses; generation itself 4.630 s) | KEEP the split-independent generation and single-family export workflow. |
| 2026-09-08 | 03:09 | Real two-CPU checkpoint roundtrip | The earlier Orbax stall was reproduced by stdlib asyncio alone: the sandbox forbade its local wakeup socket, suggesting an environment restriction rather than a checkpoint defect. | Ran the unchanged trainer checkpoint writer outside the sandbox, with a 40-second cap and temporary local output: 100-parameter CS model, `nx=16`, two forced CPU devices, optimizer/state counters at 2. Published `ckpt_1` and metadata, then restored the checkpoint. | PASS; epoch metadata, parameters, entire optimizer state, and counters matched exactly. No network, GPU, or full training. | 00:00 (5.679 s) | KEEP checkpoint implementation unchanged; earlier sandbox stalls do not establish a production defect. |
| 2026-09-08 | 03:16 | Existing paper simulations → final training arrays | Reuse the expensive simulations already generated while assigning a fresh whole-simulation split and making training independent of archived manifests. | Copied and checked all 1,315 archived files into `outputs/paper_dataset/arrays`; 7,686,144 retained rows and 73,728 accepted simulations. Seed 42 assigned train 58,984 simulations / 6,155,183 rows, validation 7,372 / 767,642, and test 7,372 / 763,319. Every physical field was finite and copied exactly; row ownership/frame metadata matched, and no simulation crossed splits. Provenance is in `outputs/paper_dataset/arrays.conversion.json`. The one-time converter is preserved in commit `04b98d6` and removed from the working tree. | PASS; original sources unchanged, 11 arrays atomically published, 95,876,969,856 bytes. No regeneration or training. | 00:20 (1,194.726 s; 02:56:47–03:16:41) | KEEP the verified arrays; do not retain a separate conversion path in the ongoing workflow. |
| 2026-09-08 | 03:20 | Full paper dataset C27 preflight | The actual multi-million-row dataset should load and normalize through the ordinary training path without historical quotas or fixed scales. | Ran `PREFLIGHT_ONLY=1 bash scripts/launch_c27_paper_dataset_full.sh` on CPU. Read 6,155,183 / 767,642 / 763,319 train/validation/test rows. Independently verified the normalization cache selection against training indices: SHA256 `62e50d390bd8560b4fae2533a321d193c4d3af3b80c06bed5c673f963adcb386`. Eta/xi/target scales were 0.1558343023 / 0.0939417481 / 0.1219815090. | PASS; finite normalized minibatch and training-only statistics on the full dataset. No model training launched. | 00:00 (40.269 s preflight; 40.449 s including verification) | KEEP the prepared dataset and C27 launch path. |
| 2026-09-08 | 03:25 | Two-step generation → NPZ directory → dataset | NPZ files already contain everything needed to combine simulations; summary files, a separate builder module, and generic artifact wrappers should be removable without changing the data. | Generated 20 real Stokes simulations under `outputs/paper_dataset_two_step_smoke_20260908_0323/generated` with batch size 8 and default seed, then ran `scripts/build_paper_dataset.py --input-root` on that directory and the CPU C27 preflight on its adjacent `arrays` directory. Generation produced four NPZ batches and no JSON. All 11 final arrays matched the earlier workflow's arrays exactly. | PASS; 20/20 attempts accepted, 16/2/2 simulation splits, finite normalized batch, no training launched. Storage/resume and failure-cleanup regression tests also pass. | 00:02 (03:23–03:25, including inspection pauses) | KEEP the direct two-step workflow; delete summary handoff, redundant modules, and the completed one-off converter. |
| 2026-09-08 | 03:50 | Functional training statistics and normalizers | Normalizers can consume the existing stats dictionary directly; optional full-dataset paths and unused statistics add complexity without serving current callers. | Compared the pre-refactor implementation from Git with the new functions entirely in memory: 17 finite cases spanning randomized magnitudes 1e-10 through 1e10, zero/constant fields, zero individual channels, and hand-written scales checking float32 rounding. All 51 chunked-extrema, 204 eager/JIT normalization-output, and 17 model-scale comparisons were exactly equal. Removed the stats class, unused metadata/time loading, optional input paths, and single-use wrappers; retained both normalization modes and their original arithmetic order. | PASS; worst difference 0.0. Persistent tests cover both modes, constant/zero fields, train-only statistics, cache selection identity, and missing/nonfinite cached scales. | 00:00 (6.257 s for numerical comparisons) | KEEP the FP refactor; normalization and model scale semantics are unchanged. |
| 2026-09-08 | 03:53 | Full-dataset preflight after utility simplification | Removing unused statistics should preserve actual C27 normalization and the exact training-row selection on the complete dataset. | Ran the CPU `PREFLIGHT_ONLY=1` launcher, recomputed the simplified cache, and compared all six normalization fields and training-selection identity to the prior cache. Train/validation/test row totals remained 6,155,183 / 767,642 / 763,319. Only normalization fields plus selection/file identity remain in the stats cache. | PASS; all six statistics and selected training rows unchanged, normalized minibatch finite. Separate two-device remainder/replication regressions also pass. No training launched. | 00:00 (28.754 s; not a controlled speed comparison) | KEEP the simpler utility and updated launcher. |
| 2026-09-08 | 14:15 | Full new-dataset C27 training and queued rollout suite | Training on every retained row from whole-simulation splits tests the simplified classical-ML workflow with the established C27 model and all three auxiliary losses; independent test-simulation rollouts measure stability beyond snapshot accuracy. | Launched `outputs/c27_paper_dataset_20260908_141423` through the existing full launcher in tmux `c27_20260908_141423`, using both RTX 6000 Ada GPUs, 40 epochs, batch 1024, LR 2e-5, mode/tangent/Hadamard weights 6/10/.01. Base revision `fe29c4c`; working-tree differences at launch were help text and generation comments only. Preflight confirmed 6,155,183 / 767,642 / 763,319 train/validation/test rows; config confirms 1,342,400 parameters and two devices. After successful training, GPU0 evaluates final and GPU1 evaluates best-validation, each on 32 test-split frame-zero ICs per current family (128 rollouts/checkpoint), batch 32, unguarded f64 integration/f32 model. Outputs: `eval_final_n32`, `eval_best_n32`; driver log: `outputs/c27_paper_dataset_20260908_141423.suite.log`. This is the current four-family suite, not the old ten-regime panel. | RUNNING; startup replicated-state checks pass, both GPUs active, initial training losses finite. Training, checkpoints, and rollout outcomes are pending. | 00:00 (launched 14:15:06; completion pending) | PENDING results; leave the two-GPU train/evaluate job running. |
| 2026-09-08 | 16:29 | Loss-only regularizer APIs and diagnostic removal | The trainer now logs only objective losses, so diagnostic dictionaries and their extra RMS, speed-error, and phase-growth calculations should be removable without changing optimization. | Compared isolated source from `1d75d22` with scalar mode/Hadamard returns and tangent `(loss, selected_count)`: 30 float32/float64 objective-and-gradient cases plus eight actual two-CPU train/eval/host profiles. Removed unused calculations and inlined single-use helpers; retained optional dispersion weighting and physical-invariant tests. | PASS; losses, gradients, selected counts, optimizer state, retained metrics, and complete epoch logs matched exactly. Disabled objectives, empty validation, replicated tails, and Hadamard firing checks passed. All 26 regularizer tests, Ruff, and repo-wide Pyright pass. Existing GPU training was untouched. | 00:00 (14.08 s direct comparisons + 28.82 s two-CPU comparisons; not a speed benchmark) | KEEP the simpler APIs and loss-only calculations. |
| 2026-09-08 | 16:53 | Shared trainer losses and batch loading | Training and validation repeat the same objectives and prefetch setup, so sharing those computations should reduce reading effort without changing optimization or two-device behavior. | Compared the trainer against `fbb6614`: eight actual two-CPU train/eval/host profiles using real JIT builders, batch loading, and prefetch; 84 metadata/resume cases; five in-memory checkpoint writer lifecycles plus an injected asynchronous failure. CLI checks cover 42 actions, 53 parse/error cases, and 57 regularizer setup comparisons. Removed redundant checkpoint arguments and CLI casts/aliases; both new helpers have two callers. Fixed literal-percent help formatting and restoration of train loss when resuming an already-completed run. | PASS; exact losses, reduced gradients, optimizer state, counters, retained metrics, and epoch logs. Checkpoint payload/publication ordering and NaN best-selection behavior preserved, except the intended completed-resume train-loss correction. All five standalone test files, Ruff, and repo-wide Pyright pass. Active GPU training untouched. | 00:00 (27.45 s two-CPU comparison; not a speed benchmark) | KEEP; trainer reduced from 1,038 to 961 working-tree lines, with no new modules or classes. |
| 2026-09-08 | 23:20 | Regularizer implementation cleanup | Repeated operator arguments, FFT/floor expressions, and single-use sampling plumbing obscure the three loss definitions without adding functionality. | Bound Hadamard operator context once, inlined its sole microbatch wrapper, removed the unused probe-scale return while retaining its RMS test, and shared repeated tangent/mode calculations. Retained all configuration options, precision conversions, numerical floors, RNG order, and small-batch behavior. Compared with `64debdb` on CPU; deleting tangent's absolute value caused small float32 rounding changes, so that expression was restored. | PASS; the accepted version matches all nine actual two-device training/evaluation profiles exactly, including gradients, optimizer state, metrics, epoch logs, and oversized-microbatch singleton tails. All 36 direct loss/gradient cases and fixed/random Hadamard probes match exactly; 192 separate mode comparisons also match exactly. All 26 regularizer tests, Ruff, and repo-wide Pyright pass. No GPU job changes. | 00:00 (17.00 s final direct comparisons + 34.27 s two-device comparison; not a speed benchmark) | KEEP; three regularizer files 482→416 lines, 69 fewer lines including trainer/test changes. |
