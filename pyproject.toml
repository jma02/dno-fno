[project]
name = "dno-fno"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "carbs @ git+https://github.com/imbue-ai/carbs.git ; python_full_version >= '3.11'",
    "h5py>=3.14.0",
    "flax>=0.11 ; python_full_version >= '3.11' and sys_platform == 'linux'",
    "jax[cuda13]>=0.7 ; python_full_version >= '3.11' and sys_platform == 'linux'",
    "matplotlib>=3.9.4",
    "numpy>=2.0.2",
    "optax>=0.2.5 ; python_full_version >= '3.11' and sys_platform == 'linux'",
    "scipy>=1.13.1",
    "torch>=2.8.0",
    "tqdm>=4.67.3",
]
